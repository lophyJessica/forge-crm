# Codex 填充指令 — WMS 报损单（BL）· 二期 · 从零新建模块

> ⚠️ 目录不存在,需新建 prd-docs/报损管理/报损单/,严格按 AGENTS 套件规范建 8 个文件,别漏别多。在 `forge-wms` 项目根跑。

```
Context:
我在给 Forge WMS(forge-wms)新建「报损单(BL)」PRD(二期)。读者研发/测试/产品复核。
报损单是库存报损核销单据,两个来源:①手动报损(货物损坏/过期/其它)②调拨到货差异自动生成(原因"调拨损耗")。涉及资产核销,必须经审核后才核销库存。
先读:
- context/:06-库存管理规则(库存状态:冻结=质检中/盘点中/报损触发;三口径 可用=现存-占用-冻结)、08-调拨流程(到货差异自动生成报损单BL,原因"调拨损耗")、09-单据命名规则(报损单 BL{YYYYMMDD}-{4位序号})、03、11-字段校验、10-通用规范
- 结构规范:AGENTS.md「PRD 套件」→ 业务单据类 8 件
- 同项目参照物(带合法审核环节的业务单据):prd-docs/盘点管理/盘点单/(状态机+审核+库存过账的写法对齐)

Request:
新建 prd-docs/报损管理/报损单/ 目录并产出标准业务单据套件 8 个文件(命名严格对齐 AGENTS 规范,勿漏勿多):
报损单主PRD.md、报损单字段清单.md、报损单_业务规则规格.md、报损单_业务流程推演.md、报损单_用例数据推演.md、报损单_Demo_列表页.md、报损单_Demo_新增编辑页.md、报损单_Demo_详情页.md
主PRD 顶部标注"本模块为二期规划"。

Output format:
- 状态机:草稿→待审核→已审核(核销过账:现存-N、生成BL类FL)/已驳回;报损审核是资产核销的合法环节(参照盘点主管审核)
- 字段清单 SSOT:报损头(报损单号BL/来源[手动/调拨差异]/关联单号[如调拨单TR]/报损类型/报损原因/申请人/审核人)+ 明细(货位·商品·报损数量)+ 系统字段
- 业务规则规格:两来源(手动新建 + 调拨差异自动生成)、报损审核、确认核销才动现存并生成FL、报损期间货物处理(如先冻结)、原因枚举含"调拨损耗"
- 用例数据推演:2026 示例,手动报损 + 调拨损耗 各一例,写清三口径走账(核销后现存-N)
- 业务流程含 Mermaid 业务流程图 + 系统时序图(核心规范5),节点换行用 `<br/>` 不用 `\n`

红线禁止:
- 不改已有:不改 context/、AGENTS.md、DESIGN.md、调拨单等其它模块(仅新建报损单目录)
- 审核适度:就一道主管/财务审核(资产核销必审,合法);别加多余的多级审批
- 不越界:确认核销才 现存-N + 生成BL类FL,草稿/待审核不动现存;三口径按 context/06;单号 BL{YYYYMMDD}-{4位序号}(context/09);调拨损耗口径对齐 context/08;Mock 2026;报损时是否先冻结等 context 未明确的标注不确定,不编造
如信息不足,标注不确定性。

Checkpoint:
除非不可逆/范围变化/缺信息,否则做完 8 个文件再汇报,列出建了哪些文件、每个写了啥。
```
