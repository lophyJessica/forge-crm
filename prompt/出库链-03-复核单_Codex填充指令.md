# Codex 填充指令 — WMS 复核单（CHECK）· 出库链-03

> 同链顺序跑：波次(01)→拣货(02)→**复核(03)**→包裹(04)→交运(05)。在 `forge-wms` 项目根跑。

```
Context:
我在做 Forge WMS(forge-wms)出库链的 PRD 补全。读者研发/测试/产品复核。
「复核单(CHECK)」是出库执行层作业单:拣货完成后,扫描包厢/商品、校验数量与订单一致;复核不通过→回退拣货重拣。是 拣货(PICK)的下游、包裹(PKG)的上游。复核本身【不做库存过账】,只校验。
出库链(context/03、05):波次WAVE→拣货PICK→复核CHECK→包裹PKG→交运DSH。
先读:
- context/:05-出库流程详解(已更新,复核在第4步、扣减在包装)、06-库存管理规则(已更新)、03-主要功能模块和清单、09-单据命名规则、11-字段校验规范、10-产品设计通用规范
- 结构规范:AGENTS.md「PRD 套件」执行作业单类
- 内容锚点:prd-docs/出库管理/出库管理主PRD.md
- 同项目参照物(执行类,结构/表格/语气对齐):prd-docs/出库管理/拣货单/ 整套 8 件

Request:
产出/补齐 prd-docs/出库管理/复核单/ 下整套 8 个文件:
复核单主PRD.md、复核单字段清单.md、复核单_业务规则规格.md、复核单_业务流程推演.md、复核单_用例数据推演.md、复核单_Demo_列表页.md、复核单_Demo_执行页.md(PDA/工作站复核作业)、复核单_Demo_详情页.md
(占位文件顶部提示头两行 HTML 注释,写作前删掉)

Output format:
按套件规范:主PRD 背景/范围/定位(来自拣货)/场景/规则摘要/验收;字段清单 SSOT(复核头/复核明细:应复核数·实复核数·差异/系统字段);业务规则规格(状态机+扫码校验+数量不符处理+复核不通过回退拣货);流程/用例带真实示例数据;Demo 页面结构不重复枚举。业务流程含 Mermaid 流程图+时序图(核心规范5)。

红线禁止:
- 不改已有:不改 context/、AGENTS.md、出库管理主PRD、上游波次/拣货文件
- 不加不该有:执行层无审核流(如 待复核→复核中→已复核,不加待审核/已审核);复核单只能由拣货完成下推,无新增入口
- 不越界:【复核不做库存过账】——不扣现存、不释放占用、不生成FL(实扣在下游"包裹完成");复核不通过→回退拣货(context/05规则);单号 CHECK 前缀按 context/09;术语三口径(现存/占用/可用);Mock 2026;拿不准标注不确定,不编造
如信息不足,标注不确定性。

Checkpoint:
除非不可逆/范围变化/缺信息,否则做完 8 个文件再汇报。
```
