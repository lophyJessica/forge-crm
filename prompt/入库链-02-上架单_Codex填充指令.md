# Codex 填充指令 — WMS 上架单（PUT）

> 用法：在 `forge-wms` 项目根启动 Codex，粘贴以下五段式指令。内容主来源是 入库管理主PRD.md（PUT 部分）。与收货单指令错开跑；建议先收货单、再上架单。

```
Context:
我在做 Forge WMS(forge-wms)入库模块 PRD 补全。读者研发/测试/产品复核。
「上架单(PUT)」是入库链路末环:PDA 扫货位,系统推荐空闲货位或人工指定,上架完成后库存转为现存可用。前置依赖质检合格的收货。
模块主PRD prd-docs/入库管理/入库管理主PRD.md 已含 PUT 的状态机(§7.2)和字段清单(§8.2),为主来源。
先读:context/ 的 04-入库流程详解、09-单据命名规则、11-字段校验规范、10-产品设计通用规范、06-库存管理规则;结构照 AGENTS.md「PRD套件」执行作业单类。

Request:
产出/补齐 prd-docs/入库管理/上架单/ 下整套 8 个文件(主PRD/字段清单/业务规则规格/业务流程推演/用例数据推演/Demo列表页/Demo执行页(PDA上架作业)/Demo详情页),内容从模块主PRD 的 PUT 部分拆出并细化。

Output format:
同套件规范;业务流程含 Mermaid 流程图+时序图;字段清单 SSOT;规则规格写状态机/货位推荐规则/库存过账时点。

红线禁止:
- 不改已有:不动模块主PRD、context/、AGENTS.md
- 不加不该有:执行层不加审核流(草稿→已确认);上架前不得跳过质检合格前置
- 不越界:上架完成才把库存从暂存/在途转「现存可用」(过账时点别写错);单号 PUT 进 context/09;术语按 context/08;Mock 2026;不编造
如信息不足,标注不确定性。

Checkpoint:
除非不可逆/范围变化/缺信息,否则做完再汇报。
```
