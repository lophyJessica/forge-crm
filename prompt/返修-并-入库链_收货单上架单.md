# 返修指令 — WMS 入库链（收货单 + 上架单）· 可并行

> `返修-并-` = 可与其它返修并行跑。在 `forge-wms` 项目根用反重力/Codex。
> **前提**：已把修好的 context/05、06 替换到本项目、已删 质检单/ 空文件夹。

```
Context:
我在返修 Forge WMS(forge-wms)入库链已生成的 PRD。六维走查发现一个贯穿性错误:把「质检」脑补成了独立单据/QC单号,但 context/03、04、09 与 入库管理主PRD 都明确"质检是收货单内环节、单据链只有 RCV→PUT、无 QC 前缀"。决策已定:质检归入收货单,质检单文件夹已删除。
先读(权威基线):
- context/04-入库流程详解(收货单 RCV 单据设计:合格数量、不合格数量/原因=RCV字段;质检=流程节点)、context/06-库存管理规则(已更新)、09-单据命名规则、11-字段校验规范、10-产品设计通用规范、03-主要功能模块和清单
- 模块主PRD:prd-docs/入库管理/入库管理主PRD.md(§7.1 RCV 状态机含 待质检/待上架/已完成/异常/已作废;§8.1 RCV 字段含合格/不合格数量/原因)
待修文件:prd-docs/入库管理/收货单/ 全部8个 + prd-docs/入库管理/上架单/ 全部8个

Request:
按下列清单返修:
1. 收货单字段清单:收回 合格数量、不合格数量、不合格原因(有不合格则必填)字段(来源=质检后填入);删除指向外部"质检单"的 qc_no 关联字段。
2. 收货单主PRD §2.2/§3:删除"不在收货单内登记质检结果""下游=质检单"等表述;质检写为 RCV 内环节(质检登记),单据链只保留 RCV→PUT。
3. 收货单状态机:按模块主PRD §7.1 对齐——至少补「已作废」(仅草稿/待收货可作废)与「已完成/已入库」终态,以及「收货中/部分收货可继续收货」的承接;不要停在 草稿→已确认。
4. 不合格闭环:有不合格必填原因(模块主PRD R6);一期仅登记不合格+原因(退货二期)。
5. 上架单字段清单/规则:合格数量来源改为"继承 收货单.合格数量",删除对"质检单/QC单号"的取数依赖(删 source_qc_no)。
6. 上架单过账时点:按已更新的 context/06——PDA 上架确认即按货位「冻结→可用」并生成 FL(每批即时);统一主PRD/业务规则/用例的措辞,消除"整单完成才转可用"vs"分批即时"的自相矛盾。
7. 全套删除对"质检单 / QC{YYYYMMDD}"单号的一切引用(context/09 无 QC 前缀)。
8. 校正 Mock:收货单 Demo 列表页/用例里的来源单号、操作人角色前后一致。

Output format:
逐文件小改,保持原套件结构与表格格式不变;业务流程若动到,保持 Mermaid 流程图+时序图完整(核心规范5)。

红线禁止:
- 不改已有:不改 context/、AGENTS.md、入库管理主PRD.md(它是上游权威,以其 §7.1/§8.1 为对齐基准,不是去改它)
- 不加不该有:不再脑补独立质检单/QC单号;执行层不加审核流(无 待审核/已审核)
- 不越界:单号 RCV/PUT 前缀严格 context/09;术语按三口径(现存/占用/可用);超收阻断保留;Mock 用 2026;拿不准标注不确定,不编造
如信息不足,标注不确定性。

Checkpoint:
除非不可逆/范围变化/缺信息,否则做完 收货单+上架单 共16文件返修再汇报,列出每处改了什么。
```
