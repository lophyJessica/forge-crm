# Codex 填充指令 — WMS 仓库档案（基础数据·并行组）

> 命名 `基础数据-并-` 的都是相互独立的档案，可任意并行（封顶 3）。在 `forge-wms` 项目根跑。

```
Context:
我在做 Forge WMS(forge-wms)基础数据模块的 PRD 补全。读者研发/测试/产品复核。
「仓库档案」是全系统的主数据地基:单一仓库单货主模式,仓库档案向下带出库区、货位。
先读:
- context/:03-主要功能模块和清单(基础数据定义)、11-字段校验规范、10-产品设计通用规范、02-上下游关联系统
- 结构规范:AGENTS.md 的「PRD 套件」章 → 档案/主数据类只出 3 件(主PRD + 字段清单 + 前端Demo版PRD)
- 可选结构参照:forge-erp 项目的 prd-docs/基础资料/仓库管理/(同为仓库档案,若能看到可对齐表格与字段风格;看不到就按 AGENTS.md 套件规范写)

Request:
产出 prd-docs/基础数据管理/仓库档案/ 下 3 个文件:
仓库档案主PRD.md、仓库档案字段清单.md、仓库档案_前端Demo版PRD.md
(占位文件顶部提示头的两行 HTML 注释,写作前删掉)

Output format:
- 主PRD:背景、功能范围(In/Out)、档案定位、字段摘要、维护规则、验收
- 字段清单 = SSOT:仓库编码/名称/类型/地址/状态等,标类型、必填、枚举、校验
- 前端Demo版PRD:列表页 + 新增编辑页 + 详情页的页面结构/控件/交互/Mock,不重复定义字段枚举

红线禁止:
- 不改已有:不改 context/、AGENTS.md、DESIGN.md;不动其它模块文件
- 不加不该有:主数据不提供物理删除,统一用「停用/启用」(AGENTS.md 核心规范4);状态变更仅按钮驱动
- 不越界:字段校验遵 context/11;术语按 context 口径;Mock 日期 2026;字段/枚举拿不准就标注不确定,不编造
如信息不足,明确标注不确定性。

Checkpoint:
除非不可逆/范围变化/缺信息,否则做完 3 个文件再汇报。
```
