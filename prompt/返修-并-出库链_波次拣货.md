# 返修指令 — WMS 出库链（波次 + 拣货单）· 可并行

> `返修-并-` = 可与其它返修并行跑。在 `forge-wms` 项目根用反重力/Codex。
> **前提**：已把修好的 context/05、06 替换到本项目。

```
Context:
我在返修 Forge WMS(forge-wms)出库链已生成的 PRD。地基已修好:context/05、06 已把出库库存扣减统一为「包装完成(PKG)触发,科目=现存-N、释放占用-N,并生成FL;可用不变」。波次/拣货 PRD 本身逻辑自洽,但当初继承了旧 context 的错误措辞"扣减可用库存/可用-N",需跟着地基改正;另有状态机对齐问题。
先读(已更新的权威基线):
- context/05-出库流程详解(已更新:扣减挂在包装完成)、context/06-库存管理规则(已更新:出库=现存-N、占用-N)、03-主要功能模块和清单、09-单据命名规则、11-字段校验规范
- 模块主PRD:prd-docs/出库管理/出库管理主PRD.md
待修文件:prd-docs/出库管理/波次/ 全部8个 + prd-docs/出库管理/拣货单/ 全部8个

Request:
1. 措辞跟改:波次 AC6 及所有文档里"扣减可用库存 / 可用-N"→改为"扣减现存、释放占用(现存-N、占用-N)",与修好的 context/05、06 一致;删除各 PRD"不确定性/待确认"章节里针对 05↔06 冲突的临时降级说明(地基已定稿,不再是不确定项)。
2. 拣货单状态机:补终止/作废态——承接"波次异常关闭 → 拣货单作废/终止"路径,不要只有 待拣→拣货中→已拣完。
3. 出库管理主PRD 状态机与子单据对齐:明确主PRD(草稿/拣货中/已拣货/已复核/已交运/已作废)描述的是哪个单据对象,或与 波次(待拣/拣货中/已完成/已关闭)、拣货(待拣/拣货中/已拣完)口径对齐,消除命名与粒度不一致。
4. pick_mode 枚举 SSOT:枚举源头留在波次(或公共字典),拣货单标"继承 WAVE"即可,不重复列枚举值。

Output format:
逐文件小改,保留套件结构与表格;动到业务流程时保持 Mermaid 流程图+时序图完整。

红线禁止:
- 不改已有:不改 context/(已定稿)、AGENTS.md、出库管理主PRD 的业务实质(仅做状态机口径对齐)
- 不加不该有:拣货执行层无审核流、无新增入口(仅波次下推)
- 不越界:术语严格三口径(现存/占用/可用),出库实扣不动可用;单号 WAVE/PICK 前缀按 context/09;Mock 2026;不编造
如信息不足,标注不确定性。

Checkpoint:
除非不可逆/范围变化/缺信息,否则做完 波次+拣货 共16文件返修再汇报。
```
