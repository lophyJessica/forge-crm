# Codex 填充指令 — WMS 供应商档案（基础数据·并行组）

> `基础数据-并-` = 相互独立可并行(封顶3)。在 `forge-wms` 项目根跑。

```
Context:
我在做 Forge WMS(forge-wms)基础数据模块的 PRD 补全。读者研发/测试/产品复核。
「供应商档案」是供应商主数据:从进销存 ERP 同步。WMS 侧用于收货单关联供应商、退货处理等。
先读:
- context/:03-主要功能模块和清单(供应商档案从 ERP 同步)、02-上下游关联系统(ERP 上游)、11-字段校验规范、10-产品设计通用规范、04-入库流程详解(收货关联供应商)
- 结构规范:AGENTS.md「PRD 套件」→ 档案/主数据类 3 件(主PRD + 字段清单 + 前端Demo版PRD)
- 参照物:forge-erp 项目 prd-docs/基础资料/供应商管理/(同一份供应商主数据,WMS 字段应与之对齐;读不到就按 AGENTS.md 档案规范写)

Request:
产出 prd-docs/基础数据管理/供应商档案/ 下 3 个文件:
供应商档案主PRD.md、供应商档案字段清单.md、供应商档案_前端Demo版PRD.md
(占位提示头两行 HTML 注释写作前删掉)

Output format:
- 主PRD:背景/范围/定位/同步机制(ERP 同步字段只读)/WMS 用途(收货/退货关联)/维护规则/验收
- 字段清单 SSOT:供应商编码、名称、简称、联系人、电话、地址、状态 等,标来源(ERP 同步/WMS 维护)、类型、必填、枚举、校验
- 前端Demo版PRD:列表/新增编辑/详情页;ERP 同步字段只读

红线禁止:
- 不改已有:不改 context/、AGENTS.md、DESIGN.md、其它模块
- 不加不该有:主数据不物理删除,用「停用/启用」;状态仅按钮驱动;ERP 同步字段 WMS 侧只读
- 不越界:单据中供应商信息为快照存储(改档案不影响已有单据);字段与 ERP 供应商档案对齐;术语一致;Mock 2026;context 未明确项标注不确定,不编造
如信息不足,标注不确定性。

Checkpoint:
除非不可逆/范围变化/缺信息,否则做完 3 个文件再汇报。
```
