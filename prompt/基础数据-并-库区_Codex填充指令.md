# Codex 填充指令 — WMS 库区（基础数据·并行组）

> 命名 `基础数据-并-` 的都是相互独立的档案，可任意并行（封顶 3）。在 `forge-wms` 项目根跑。

```
Context:
我在做 Forge WMS(forge-wms)基础数据模块的 PRD 补全。读者研发/测试/产品复核。
「库区」是仓库内的功能分区:收货区/存储区/拣货区/退货区/发货区,是货位的上级。
先读:
- context/:03-主要功能模块和清单(库区=收货区/存储区/拣货区/退货区/发货区)、11-字段校验规范、10-产品设计通用规范
- 结构规范:AGENTS.md「PRD 套件」→ 档案/主数据类 3 件(主PRD + 字段清单 + 前端Demo版PRD)

Request:
产出 prd-docs/基础数据管理/库区管理/ 下 3 个文件:
库区主PRD.md、库区字段清单.md、库区_前端Demo版PRD.md
(占位提示头两行 HTML 注释写作前删掉)

Output format:
- 主PRD:背景、范围、定位(隶属仓库)、库区类型枚举(收货/存储/拣货/退货/发货)、维护规则、验收
- 字段清单 = SSOT:库区编码/名称/类型/所属仓库/状态等,标类型、必填、枚举、校验;所属仓库来源=仓库档案
- 前端Demo版PRD:列表/新增编辑/详情页结构与交互,不重复字段枚举

红线禁止:
- 不改已有:不改 context/、AGENTS.md、DESIGN.md、其它模块
- 不加不该有:主数据不物理删除,用「停用」;状态仅按钮驱动
- 不越界:「所属仓库」字段来源必须是仓库档案(上游 SSOT),别自建;库区类型严格按 context/03 五类;校验遵 context/11;Mock 2026;拿不准标注不确定,不编造
如信息不足,标注不确定性。

Checkpoint:
除非不可逆/范围变化/缺信息,否则做完 3 个文件再汇报。
```
