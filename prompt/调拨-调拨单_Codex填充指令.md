# Codex 填充指令 — WMS 调拨单（TR）· 二期规划 · 独立可并行

> ⚠️ context/08 明确调拨是【二期内容】——本 PRD 标注为二期规划。在 `forge-wms` 项目根跑。

```
Context:
我在做 Forge WMS(forge-wms)调拨管理的 PRD 补全(二期规划)。读者研发/测试/产品复核。
「调拨单(TR)」是仓间调拨的分步单据:创建→调出库确认出库(扣现存、改在途)→调入库收货清点→实收=调出则确认入库(加现存、消在途);实收≠调出则按实收入库并生成报损单(BL)记差异。类型:仓间调拨 / ERP下发调拨。
先读:
- context/:08-调拨流程(权威,注意开头"二期内容")、06-库存管理规则(在途=调出确认→调入确认之间,在途不可销售)、09-单据命名规则(调拨单 TR{YYYYMMDD}-{4位序号};差异报损单 BL{YYYYMMDD}-{4位序号})、02-上下游关联系统(ERP 调拨对接)、11、10、03
- 结构规范:AGENTS.md「PRD 套件」执行作业单类(8件)
- 同项目执行类参照:prd-docs/出库管理/拣货单/

Request:
产出/补齐 prd-docs/调拨管理/调拨单/ 下整套 8 个文件(主PRD 顶部标注"二期规划"):
调拨单主PRD、字段清单、_业务规则规格、_业务流程推演、_用例数据推演、_Demo_列表页、_Demo_执行页、_Demo_详情页

Output format:
- 主PRD:开头标注"本模块为二期规划";功能范围写清一期不做/二期做
- 状态机(按 context/08 分步调拨):草稿→已调出(在途)→已调入(完成) ;调入差异分支
- 字段清单 SSOT:调拨头(调出仓/调入仓/调拨类型/来源=WMS手动或ERP下发)+ 明细(商品·调出数·实收数·差异)+ 系统字段
- 业务规则规格:调出确认(现存-N、在途+N)、在途不可销售、调入确认(现存+N、在途-N)、实收≠调出→按实收入库+生成报损单(BL,原因"调拨损耗")
- 用例数据推演:2026 示例走账——正常调拨(在途流转)、到货差异(生报损单)各一例,写清两仓 现存/在途 变化
- 业务流程含 Mermaid 流程图+时序图(核心规范5)

红线禁止:
- 不改已有:不改 context/、AGENTS.md
- 不越界:在途是 context/06 定义的状态(调出确认→调入确认之间,不可销售);差异走报损单 BL(前缀 context/09);单号 TR 按 context/09;术语三口径+在途;Mock 2026;不编造
- 报损单(BL)本次不产出其完整套件,只在调拨单里写清"差异触发生成 BL"的接口口径,标注 BL 独立套件待补
如信息不足,标注不确定性。

Checkpoint:
除非不可逆/范围变化/缺信息,否则做完 8 个文件再汇报。
```
