# Codex 填充指令 — WMS 波次（WAVE）

> 用法：在 `forge-wms` 项目根启动 Codex，粘贴以下五段式指令。与拣货单指令错开跑，勿同时改同目录。

```
Context:
我在做 Forge WMS(forge-wms)出库模块的 PRD 套件补全。读者是研发/测试/产品复核。
「波次(WAVE)」是出库作业的组织单元:系统波次(按承运商/线路/优先级自动聚合多个出库需求)+ 手动波次,是 拣货→复核→包装→交运 的起点。
出库链路(context/03):波次(WAVE)→拣货(PICK)→复核(CHECK)→包裹(PKG)→交运(DSH)。
两依赖已就位,请先读:
- 业务地基 context/:05-出库流程详解(主来源)、03-主要功能模块和清单、06-库存管理规则、09-单据命名规则、11-字段校验规范、10-产品设计通用规范、02-上下游关联系统
- 结构规范:AGENTS.md 的「PRD 套件」章(业务单据类各文件职责)+ 「查询页 vs 业务单据」判断清单
- 内容锚点:prd-docs/出库管理/出库管理主PRD.md(模块概览,含单据体系与状态机)
- 同项目参照物(若已填好):prd-docs/入库管理/收货单 或 上架单 的成套写法可对齐语气与表格格式

Request:
产出/补齐 prd-docs/出库管理/波次/ 下整套 8 个文件:
波次主PRD.md、波次字段清单.md、波次_业务规则规格.md、波次_业务流程推演.md、波次_用例数据推演.md、波次_Demo_列表页.md、波次_Demo_新增编辑页.md、波次_Demo_详情页.md
(占位文件顶部有「状态/角色/参照套件」提示头,写作前删掉那两行 HTML 注释)

Output format:
按 AGENTS.md 套件规范:
- 主PRD:背景/功能范围(In·Out Scope)/单据定位/业务场景/状态机摘要/验收重点
- 字段清单 = SSOT:分「波次头/波次明细(关联的出库需求行)/系统字段」,标来源(系统聚合/用户选/系统算)、类型、必填、枚举
- 业务规则规格:状态机(如 待拣→拣货中→已完成 / 可关闭)+ 波次聚合规则(系统按什么维度聚合、手动波次如何组)+ 权限 + 库存占用规则
- 业务流程推演:带真实示例数据的动态沙盘(一个波次聚合 N 个出库需求的全过程)
- 用例数据推演:带数字的公式/状态快照/走查清单
- 3 个 Demo:页面结构/控件/交互/Mock,不重复定义字段枚举
- 业务流程必须含 Mermaid 业务流程图 + 系统时序图(AGENTS.md 核心规范5)

红线禁止:
- 不改已有:不动 出库管理主PRD.md(模块概览,拆内容是"引用+细化"不搬走删除);不改 context/、AGENTS.md、DESIGN.md
- 不加不该有:不给波次加多余审核流;波次是组织单元,别把拣货/复核的执行细节塞进来(那是下游单据)
- 不越界发挥:库存「占用/扣减」的过账时点严格按 context/05、06——波次生成时是否占用库存、何时转实扣,拿不准就标注不确定,别自己拍板;单号 WAVE 前缀须在 context/09 有定义,没有先标注别自创;术语严格按 context/08 口径(现存/占用/可用);Mock 日期用 2026;数据冲突以 context/简历为准,不编造
如信息不足,明确标注不确定性。

Checkpoint:
除非涉及不可逆操作、范围变化或需要我补信息,否则一次做完 8 个文件再汇报,附一句每个文件写了啥 + 你对过账时点等不确定项的标注。
```
