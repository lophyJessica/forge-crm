# Codex 填充指令 — WMS 货位（基础数据·并行组）

> 命名 `基础数据-并-` 的都是相互独立的档案，可任意并行（封顶 3）。在 `forge-wms` 项目根跑。

```
Context:
我在做 Forge WMS(forge-wms)基础数据模块的 PRD 补全。读者研发/测试/产品复核。
「货位」是最细粒度的库存承载位置:货架编号/层/列,支持条码绑定,隶属库区。上架(PUT)/拣货(PICK)都靠扫货位条码作业。
先读:
- context/:03-主要功能模块和清单(货位=货架编号/层/列,支持条码绑定)、11-字段校验规范、10-产品设计通用规范、04-入库流程详解(上架扫货位)、05-出库流程详解(拣货扫货位)
- 结构规范:AGENTS.md「PRD 套件」→ 档案/主数据类 3 件(主PRD + 字段清单 + 前端Demo版PRD)

Request:
产出 prd-docs/基础数据管理/货位管理/ 下 3 个文件:
货位主PRD.md、货位字段清单.md、货位_前端Demo版PRD.md
(占位提示头两行 HTML 注释写作前删掉)

Output format:
- 主PRD:背景、范围、定位(隶属库区)、货位编码规则(货架/层/列)、条码绑定、维护规则、验收
- 字段清单 = SSOT:货位编码/货架/层/列/所属库区/条码/状态/是否可用等,标类型、必填、枚举、校验;所属库区来源=库区档案
- 前端Demo版PRD:列表/新增编辑/详情页,含条码绑定交互,不重复字段枚举

红线禁止:
- 不改已有:不改 context/、AGENTS.md、DESIGN.md、其它模块
- 不加不该有:主数据不物理删除,用「停用」;状态仅按钮驱动
- 不越界:「所属库区」字段来源必须是库区档案(上游 SSOT);货位编码/条码规则若 context 未明确就标注不确定,别自创;校验遵 context/11;术语一致;Mock 2026;不编造
如信息不足,标注不确定性。

Checkpoint:
除非不可逆/范围变化/缺信息,否则做完 3 个文件再汇报。
```
