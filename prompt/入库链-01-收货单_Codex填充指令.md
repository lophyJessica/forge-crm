# Codex 填充指令 — WMS 收货单（RCV）

> 用法：在 `forge-wms` 项目根启动 Codex，粘贴以下五段式指令。内容主来源是 252 行的 入库管理主PRD.md（RCV 部分）。与上架单指令错开跑。

```
Context:
我在做 Forge WMS(forge-wms)的入库模块 PRD 拆分与补全。读者是研发/测试/产品复核。
「收货单(RCV)」是入库链路第一环:PC 创建、关联采购订单 PO、打印收货标签,后接质检→上架。
现有 prd-docs/入库管理/入库管理主PRD.md(252行,模块级)已含 RCV 的状态机(§7.1)和字段清单(§8.1),是本次内容的主来源。
先读:
- context/:04-入库流程详解、09-单据命名规则、11-字段校验规范、10-产品设计通用规范、02-上下游关联系统、06-库存管理规则
- 结构规范:AGENTS.md 的「PRD 套件」章(执行作业单类各文件职责)
- 内容锚点:prd-docs/入库管理/入库管理主PRD.md 里 RCV 相关章节

Request:
从模块主PRD 拆出收货单专属内容,产出/补齐 prd-docs/入库管理/收货单/ 下整套 8 个文件:
收货单主PRD.md(单据级,从模块PRD抽RCV部分)、收货单字段清单.md、收货单_业务规则规格.md、收货单_业务流程推演.md、收货单_用例数据推演.md、收货单_Demo_列表页.md、收货单_Demo_执行页.md(即PDA/PC收货作业页)、收货单_Demo_详情页.md

Output format:
按 AGENTS.md 套件规范:主PRD 写背景/范围/单据定位/场景/规则摘要/验收;字段清单为 SSOT;业务规则规格写状态机+校验+计算;流程/用例带真实示例数据;Demo 写页面结构不重复枚举。业务流程必须含 Mermaid 业务流程图 + 系统时序图(AGENTS.md 核心规范5)。

红线禁止:
- 不改已有:不动 入库管理主PRD.md(它是模块概览,拆内容是"引用+细化"不是搬走删除);不改 context/、AGENTS.md
- 不加不该有:收货是执行层,状态只「草稿→已确认」不加审核流;超收要阻断(context/04);质检未过不可流转到上架
- 不越界:单号 RCV 前缀须在 context/09 定义;字段校验遵 context/11;术语按 context/08 口径;Mock 用 2026;数据冲突以简历/context为准,不编造
如信息不足,标注不确定性。

Checkpoint:
除非不可逆/范围变化/缺信息,否则做完 8 个文件再汇报。
```
