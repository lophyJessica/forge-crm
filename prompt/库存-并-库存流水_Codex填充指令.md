# Codex 填充指令 — WMS 库存流水（查询页·并行组）

> `库存-并-` = 查询页之间完全独立,可任意并行(封顶3)。在 `forge-wms` 项目根跑。

```
Context:
我在做 Forge WMS(forge-wms)库存管理模块的 PRD 补全。读者研发/测试/产品复核。
「库存流水(FL)」是查询页(PDA/PC):自动记录所有库存变动,按仓库/货位/商品/时间段/变动类型多维度查询。每条流水=时间+仓库+货位+商品+变动类型+变动数量(±)+变动后现存。
库存公式:可用=现存-占用-冻结 (context/06)。流水为**只读查询**,不创建、不修改。
先读:
- context/:06-库存管理规则(流水定义:每次变动生成一条,含 FL 单号)、03、11、10、09-单据命名规则(FL 前缀={YYYYMMDDHHmmss}{8位毫秒})
- 结构规范:AGENTS.md→ 查询页6件
- 参照物:forge-erp 项目 prd-docs/库存管理/库存流水查询/;同项目 库存查询/ 查询页写法

Request:
产出 prd-docs/库存管理/库存流水/ 下 6 个文件:
库存流水主PRD.md、库存流水字段清单.md、库存流水_业务规则规格.md、库存流水_业务流程推演.md、库存流水_用例数据推演.md、库存流水_Demo_列表页.md

Output format:
查询页规范:主PRD 状态机写"不适用(只读查询流水,无单据状态流转)";字段清单 SSOT 分「查询条件」「列表展示」「系统字段」;只列表页。

红线禁止:
- 不改已有、不加不该有:只读查询、不加新增/编辑/详情/调整;不脑补状态机;不加"反审核"等动作
- 不越界:FL 单号格式 context/09({YYYYMMDDHHmmss}{8位毫秒},共22位);变动类型枚举(入库+/出库-/调拨±/盘盈+/盘亏-/冻结/解冻/占用/释放)需在 context/06 或库存流水模块字段清单有定义;术语三口径;Mock 2026;不编造
如信息不足,标注不确定性。

Checkpoint:
除非不可逆/范围变化/缺信息,否则做完 6 个文件再汇报。
```
