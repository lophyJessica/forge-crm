# Codex 填充指令 — WMS 拣货单（PICK）

> 用法：在 `forge-wms` 项目根启动 Codex，粘贴以下五段式指令。与波次指令错开跑，勿同时改同目录。

```
Context:
我在做 Forge WMS(forge-wms)出库模块的 PRD 套件补全。读者是研发/测试/产品复核。
「拣货单(PICK)」是出库执行层作业单:PDA 按波次领任务 → 扫描货位 → 扫描商品 → 确认拣货,是 波次 的下游、复核 的上游。
出库链路(context/03):波次(WAVE)→拣货(PICK)→复核(CHECK)→包裹(PKG)→交运(DSH)。
两依赖已就位,请先读:
- 业务地基 context/:05-出库流程详解(主来源)、03-主要功能模块和清单、06-库存管理规则、09-单据命名规则、11-字段校验规范、10-产品设计通用规范
- 结构规范:AGENTS.md 的「PRD 套件」章(执行作业单类各文件职责)
- 内容锚点:prd-docs/出库管理/出库管理主PRD.md(模块概览,含单据体系与状态机);上游 prd-docs/出库管理/波次(拣货任务来自波次)
- 同项目参照物(若已填好):prd-docs/入库管理/收货单 或 上架单(同为 PDA 执行作业类,结构/表格/语气对齐)

Request:
产出/补齐 prd-docs/出库管理/拣货单/ 下整套 8 个文件:
拣货单主PRD.md、拣货单字段清单.md、拣货单_业务规则规格.md、拣货单_业务流程推演.md、拣货单_用例数据推演.md、拣货单_Demo_列表页.md、拣货单_Demo_执行页.md(即 PDA 拣货作业页)、拣货单_Demo_详情页.md
(占位文件顶部提示头的两行 HTML 注释,写作前删掉)

Output format:
按 AGENTS.md 套件规范(执行作业单类):
- 主PRD:背景/范围/单据定位(来自波次)/场景/规则摘要/验收
- 字段清单 = SSOT:分「拣货头(关联波次/拣货员)/拣货明细(货位·商品·应拣数·实拣数)/系统字段」,标来源、类型、必填、枚举
- 业务规则规格:状态机(草稿/待拣→拣货中→已拣完,执行层不加审核流)+ 扫码校验(扫错货位/错商品/超拣拦截)+ 缺货处理 + 库存过账规则
- 业务流程推演:带真实示例数据的沙盘(PDA 领任务→扫货位→扫商品→确认 全过程)
- 用例数据推演:带数字的公式/状态快照/走查清单(含缺货、拣错等异常)
- Demo:列表页 + PDA 执行页(扫码交互重点) + 详情页;不重复定义字段枚举
- 业务流程必须含 Mermaid 业务流程图 + 系统时序图(AGENTS.md 核心规范5)

红线禁止:
- 不改已有:不动 出库管理主PRD.md、波次相关文件、context/、AGENTS.md、DESIGN.md
- 不加不该有:拣货是执行层,状态只「草稿/待拣→拣货中→已拣完」,严禁「待审核/已审核」;不加"新增"入口——拣货任务必须由上游波次下推,不可凭空创建
- 不越界发挥:库存「占用→实扣」的过账时点严格按 context/05、06——是拣货确认时实扣还是复核/出库后实扣,拿不准就标注不确定,别自己拍板;单号 PICK 前缀须在 context/09 有定义,没有先标注别自创;术语按 context/08(现存/占用/可用);Mock 日期用 2026;数据冲突以 context/简历为准,不编造
如信息不足,明确标注不确定性。

Checkpoint:
除非涉及不可逆操作、范围变化或需要我补信息,否则一次做完 8 个文件再汇报,附每个文件要点 + 过账时点等不确定项的标注。
```
