# Codex 填充指令 — WMS 包裹（PKG）· 出库链-04 ⭐库存扣减发生点

> 同链顺序跑：…复核(03)→**包裹(04)**→交运(05)。在 `forge-wms` 项目根跑。
> ⚠️ 本单是出库链【库存实扣】的唯一发生点，过账口径务必按已修好的 context/05、06。

```
Context:
我在做 Forge WMS(forge-wms)出库链的 PRD 补全。读者研发/测试/产品复核。
「包裹(PKG)」是出库执行层作业单:复核通过后称重、贴面单、生成包裹号。**这是整条出库链库存实扣的唯一发生点**——包装完成时:现存-N、释放占用-N、生成库存流水FL(可用不变,因占用已在 SO 审核时从可用扣过)。是 复核(CHECK)的下游、交运(DSH)的上游。
出库链(context/03、05):波次→拣货→复核→包裹→交运。
先读(地基已更新,务必以最新为准):
- context/05-出库流程详解(流程图:扣减/FL 挂在"包装完成 Pack";业务规则:"包装完成=库存扣减触发点,现存-N、释放占用-N,生成FL,可用不变")、06-库存管理规则(出库-=包装完成过账,现存-N、占用-N)、03、09、11、10
- 结构规范:AGENTS.md「PRD 套件」执行作业单类
- 内容锚点:prd-docs/出库管理/出库管理主PRD.md
- 同项目参照物:prd-docs/出库管理/拣货单/ 整套 8 件

Request:
产出/补齐 prd-docs/出库管理/包裹/ 下整套 8 个文件:
包裹主PRD.md、包裹字段清单.md、包裹_业务规则规格.md、包裹_业务流程推演.md、包裹_用例数据推演.md、包裹_Demo_列表页.md、包裹_Demo_执行页.md(称重/贴面单/生成包裹号作业)、包裹_Demo_详情页.md
(占位提示头两行 HTML 注释写作前删掉)

Output format:
按套件规范。**业务规则规格必须把库存过账写清**:确认包装完成时触发「现存-N、释放占用-N、生成FL」,并给出三口径变化的用例数字推演(在 用例数据推演 里用 2026 示例走账:出库前后 现存/占用/可用 各是多少)。字段清单 SSOT(包裹头:包裹号/承运商/重量/面单;明细:商品·出库数量/系统字段)。业务流程含 Mermaid 流程图+时序图(核心规范5)。

红线禁止:
- 不改已有:不改 context/(已定稿)、AGENTS.md、出库管理主PRD、上游复核/拣货文件
- 不加不该有:执行层无审核流;包裹只能由复核通过下推,无新增入口
- 不越界【过账铁律】:实扣科目严格「现存-N、释放占用-N」——**绝不写"可用-N"**(可用在 SO 审核占用时已减过,重复扣会对账错);FL 在包装完成时生成一条;交运(下游)不再触发任何库存过账;单号 PKG/FL 前缀按 context/09;术语三口径;Mock 2026;不编造
如信息不足,标注不确定性。

Checkpoint:
除非不可逆/范围变化/缺信息,否则做完 8 个文件再汇报,重点说明库存过账那段怎么写的。
```
