# Codex 填充指令 — WMS 商品档案（基础数据·并行组）⭐库存预警的上游

> `基础数据-并-` = 相互独立可并行(封顶3)。在 `forge-wms` 项目根跑。

```
Context:
我在做 Forge WMS(forge-wms)基础数据模块的 PRD 补全。读者研发/测试/产品复核。
「商品档案」是全系统商品主数据:基础信息从进销存 ERP 同步,WMS 侧额外维护仓储属性。收货/上架/拣货的商品快照、库存预警的安全/最低库存都来自这里,是关键上游 SSOT。
先读:
- context/:03-主要功能模块和清单(商品/供应商档案从 ERP 同步)、02-上下游关联系统(ERP 为上游)、11-字段校验规范、10-产品设计通用规范、06-库存管理规则
- 结构规范:AGENTS.md「PRD 套件」→ 档案/主数据类 3 件(主PRD + 字段清单 + 前端Demo版PRD)
- 参照物:forge-erp 项目 prd-docs/基础资料/商品管理/(同一份商品主数据,WMS 字段应与之对齐;读不到就按 AGENTS.md 档案规范写)

Request:
产出 prd-docs/基础数据管理/商品档案/ 下 3 个文件:
商品档案主PRD.md、商品档案字段清单.md、商品档案_前端Demo版PRD.md
(占位提示头两行 HTML 注释写作前删掉)

Output format:
- 主PRD:背景/范围/定位/同步机制(ERP 同步来的字段只读、WMS 仓储属性可维护)/维护规则/验收
- 字段清单 SSOT,分两组:
  ① ERP 同步字段(只读):商品编码、名称、规格、单位、条码、分类、税率等
  ② WMS 仓储属性(WMS 维护):安全库存、最低库存、默认货位、体积、重量、是否批次管理、状态 等
  每字段标来源、类型、必填、枚举、校验
- 前端Demo版PRD:列表/新增编辑/详情页;编辑页需体现"ERP 同步字段只读、仓储属性可改"

红线禁止:
- 不改已有:不改 context/、AGENTS.md、DESIGN.md、其它模块
- 不加不该有:主数据不物理删除,用「停用/启用」;状态仅按钮驱动;ERP 同步字段在 WMS 侧只读不可编辑
- 不越界:安全库存/最低库存字段务必落地(库存预警在引用它);单据中商品信息为快照存储(改档案不影响已有单据);术语三口径;Mock 2026;字段/枚举 context 未明确的标注不确定,不编造
如信息不足,标注不确定性。

Checkpoint:
除非不可逆/范围变化/缺信息,否则做完 3 个文件再汇报。
```
