# Forge CRM 客户关系管理系统

> 项目类型：B端 CRM | 角色：产品经理 | 品牌：Forge（系统与演示公司均为 Forge，不出现旧品牌）
> 基于京东零售 CRM 实战经验（线索评分模型、转化率 35%→53%、2800 条线索 1500 份问卷），以 Forge 为演示品牌还原。
>
> **当前分支（0913test）**：项目框架升级版。结构对齐维他命 QS-JXC-Project 分层（01~99）+ 版本化 + 原型标注闭环。`main` 为旧扁平结构基线，合并前先在本分支验证。

## 项目简介

Forge CRM 覆盖线索管理、商机管理、客户管理、跟进记录、AI 评分预测五大模块。向上对接 ERP 客户档案与销售订单，向下串联 WMS 发货履约。核心验证方向：**AI 串联 CRM 和 ERP**，实现线索评分→自动转客户→商机预测→触发销售的闭环。

## 项目结构与资料职责（升级框架）

- `00-草稿对话/`：讨论稿、临时输入，只作线索，不直接视为口径。
- `01-全局背景信息/`：业务背景/项目范围/上下游系统/模块地图/核心流程/单据命名/AI评分规则/产品设计规范 等全局基线。
- `02-需求调研/`：各业务模块的场景、痛点、一期范围与初步方案。
- `03-产品设计/`：正式设计产物，**按版本分目录**（`<日期>-<版本说明>/`）。版本下含：字段清单TSV（初稿+详细稿）、系统架构与流程、各模块 = 主PRD + 页面骨架 + 前端Demo_PRD合集（列表/新增编辑/详情）+ `annotations/`（原型标注）。
- `04-常用模版/`：新建产物用的结构模板，不是业务事实源。
- `05-提示词合集/`：AI 协作提示词，可复用工作方式，不替代当前需求。
- `06-产品方法论/`：产出顺序、文档分层与走查方法。
- `07-UI规范库/`：页面 Token、组件、交互、状态、原型复刻规范。
- `08-相关资料/`：审计/走查/过程报告（check-reports/、audit-reviews/）。
- `99-产品原型prototype/`：React+TypeScript+Vite 前端 Demo；Mock 数据只服务演示，不代表数据库设计。

## 核心规范

1. 所有文档简体中文，字段名/组件名/枚举值保留英文。
2. 业务 PRD 必含：业务背景、功能范围(In/Out)、单据定位、业务场景、状态机、字段清单。
3. 状态变更必须通过动作按钮触发，不允许在表单直接修改状态字段。
4. 主数据不提供删除，统一"停用"替代。
5. 数据冲突以简历为准（京东 CRM：线索首次响应 1.5h→30min、转化率 35%→53%、2800 线索 1500 问卷）。

## 权威源与冲突处理

按信息职责取值，不用一份文档覆盖所有问题：
- 跨模块口径以 `01-全局背景信息/` 为准；跨角色流程以业务流程为准，跨单据处理以系统单据流程为准。
- 字段名称/类型/来源/枚举/字段级约束 → **字段清单详细稿 TSV**（确认后唯一源；确认前初稿仅临时输入）。
- 模块内状态/动作/业务规则/回写/异常 → **业务 PRD**，且不得与全局规则或跨单据流程冲突。
- 页面结构/控件/显隐/校验时机/反馈 → **前端 Demo PRD** + `07-UI规范库/`，不反向从 Mock 发明业务规则。

发现冲突先列文件证据和影响范围；未确认的标"待确认"，用户拍板后再同步权威源及受影响下游。**AI 生成时发现缺失或冲突，先列出问题，不自行补成已确认事实；不把猜测包装成业务口径。**

## AI 协作任务规范（给 AI 下任务）
**先给边界，再给任务；一次只让 AI 输出一个层次**（字段/业务规则/页面交互/原型四层分开，不同时生成，避免把不同层内容混在一起）。可复用四段式：

```
输入材料：这次需读取哪些 01全局规则 / 03流程 / 字段清单TSV(详细稿) / 相关PRD / 07UI规范。
当前任务：只输出 <业务PRD | 页面骨架 | 列表页DemoPRD | 某页前端实现> 中的一种。
权威边界：字段以TSV、业务规则以业务PRD、页面交互以DemoPRD+07UI规范；页面骨架只表达结构不新增规则。
输出要求：列改动文件 + 影响哪些下游（字段TSV/业务PRD/DemoPRD/其他页面），不重复维护上游已确认内容。
```

**三层约束拆分**（避免"字段约束"重复）：字段事实（类型/来源/枚举/格式→TSV）、业务约束（审核后不可改/数量上限→业务PRD）、页面约束（失焦校验/控件形式→DemoPRD）。同一字段三段分属三层，不是重复。

## 验证门（分类型质量门，build≠运行态）
每次生成一组产出物后，至少过一遍对应检查：
- **文档**：相对链接/标题层级/术语/旧口径残留正常；Mermaid 语法与闭环；主PRD 的流程、关联入口可打开且各司其职。
- **字段清单**：表头/列数/枚举/必填性正确；字段未被业务PRD/DemoPRD 重复定义或改写；下游投影同步。
- **前端**：`npm run build` 必过；**页面/路由变化还要 `npm run dev` 人工走查** 列表/详情/新增/编辑各状态（build 和单测 ≠ 页面运行态）；`data-anno` 标注锚点未被重构清掉。
- **全量**：`git diff --check`；不混入无关改动；字段/状态/动作变化后旧名旧规则不残留下游。

## 技术栈（Demo 版）与前端原型

- 前端：React + TypeScript + Tailwind CSS / antd（按模块），构建 Vite。
- 命令在 `99-产品原型prototype/` 执行：`npm install` / `npm run dev` / `npm run build` / `npm run preview`。
- 原型标注：`99-产品原型prototype/` 内的 prototype-annotation runtime + `03-产品设计` 各模块 `annotations/`（详见 06-产品方法论 与 prototype-annotation 检阅）。

## 相关系统与系统边界（铁律）

| 系统 | 关系 | 说明 |
|------|------|------|
| ERP（含销售订单） | 下游 | 客户档案 SSOT 在 ERP；CRM 商机成交后下推销售订单 |
| WMS | 间接下游 | ERP 发货单下发 WMS，CRM 不直接对 WMS |

| 数据 | SSOT（唯一权威源） | CRM 可以做什么 |
|------|-------------------|---------------|
| 客户档案 | **ERP** | CRM 持客户快照、跟进记录、商机关联；正式建档必须写 ERP |
| 商品/价格 | **ERP** | CRM 商机阶段可引用，不独立维护 |
| 线索 / 商机 | **CRM** | 唯一权威源 |
| 订单 | **ERP** | CRM 商机成交后下推 ERP 销售订单草稿 |

## AI 串联层（核心验证）

| AI 节点 | 输入 | 输出 | 动作 |
|---------|------|------|------|
| 线索评分 | 历史跟进+转化结果 | 0-100 分 | 高分自动分配、低分进培育池 |
| 商机预测 | 客户画像+历次订单+跟进频率 | 成交概率+推荐商品 | 预测成交→ERP 库存预占；预测流失→CRM 召回任务 |
| 流失预警 | 跟进间隔+订单停滞天数 | 流失风险等级 | 高风险→跟进提醒+ERP 信用冻结 |

串联方式：API 实时同步（Mock 模拟）。

## 文档与变更工作方式

动手前先判断本次改的是业务/字段/页面/方法论/代码，只读相关权威资料。修改传播方向：
- 字段变化：先改详细稿 TSV → 查业务PRD/页面骨架/DemoPRD/Mock/原型。
- 状态/动作/业务结果变化：先改全局规则/流程/业务PRD → 查字段清单及所有页面产物。
- 页面布局/控件/反馈变化：改 DemoPRD/UI规范/原型，不反向改写业务口径。
- 模板/提示词/方法论变化：仅维护对应资产，不批量改写既有业务文档。

精准修改，不顺手重构无关内容。中文用中文标点；保留原 Markdown 标题/表格/链接/Mermaid/`:::info` 结构。TSV 保持列数与表头，不增加说明行。

## 约束（NIS：Not In Scope）

- 不涉及真实后端 API，AI 串联用 Mock 模拟。
- 不涉及 ERP/WMS 侧原型修改，只做 CRM 侧，对接走 Mock。
- 不涉及移动端（App/小程序），仅 PC Web。
- 不涉及市场营销模块（短信/邮件群发、广告归因）。

## 产物自动流转管道（完成后必做，路径已按 99-产品原型prototype 更新）

1. Mac 项目目录先 `git pull --ff-only`，再构建。
2. 在 `99-产品原型prototype/` 执行 `npm run build`，确认 `dist/` 生成。
3. dist 内部打包（zip 解压第一层即 index.html），唯一文件名 `forge-crm-<任务>-<时间戳>.zip`。
4. 上传 VPS incoming：`rsync -avz -e "ssh -p 2222 -i ~/.ssh/id_ed25519_vps" <zip> root@<VPS>:/var/www/pmlophy.com/forge-crm-incoming/`。
5. 回报远端文件名/字节数/时间戳；cron 只检测不自动部署。
6. 自检报告存 `08-相关资料/` + 上传 ai-reports 管道。
7. 用户确认后父会话执行 `bash /root/deploy-all.sh --deploy forge-crm`。

**状态区分：**上传 incoming ≠ 部署；上传报告 ≠ 部署；HTTP 200 也不能单独证明新包已上线。父会话必须独立核对线上 index.html 时间戳、bundle 名称/时间、部署日志、HTTP 状态。红线：不 commit / 不 push（除本分支框架升级按需 commit）。

## 报告统一归档（铁律）

- 所有自检/审计/复核/部署报告统一放 `08-相关资料/`（check-reports/、audit-reviews/），禁止散落在项目根。
- 命名：`{项目}-{任务}-{YYYYMMDD}.md`。