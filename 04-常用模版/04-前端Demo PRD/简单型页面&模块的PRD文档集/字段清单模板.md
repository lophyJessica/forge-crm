# [模块名]字段清单

> **文档定位**：[模块名]字段定义的唯一真相源（SST）。业务PRD和Demo PRD只引用字段名称，不重复维护枚举、必填性和字段口径。
> **适用类型**：外部只读资料、纯查询页、无复杂状态机的轻量配置或极简维护模块。
> **维护形态**：若为轻量配置（工作台+弹窗），「详情展示」=查看弹窗，「新增页/编辑页」=弹窗可编辑；备注写明 `轻量配置：查看=弹窗`。勿仅为轻量配置增加操作日志字段投影。详规见 `07-UI规范库/08-轻量配置与弹窗表单约定.md`。
> **成熟度**：粗纲 / 定稿
> **版本**：V0.x（粗纲）/ V1.0+（定稿）
> **更新时间**：YYYY-MM-DD

---

## 0. 成熟度规则

| 成熟度 | 保留内容 | 能否作为完整SST |
| :--- | :--- | :---: |
| 粗纲 | 文首信息 + 粗纲简表 + 必要的数据边界 | 否 |
| 定稿 | 概述 + 完整字段表 + 枚举、校验和Changelog | 是 |

升级定稿时删除整节“粗纲简表”，不得长期维护两套字段表。

---

## 粗纲简表（仅粗纲保留）

| 结构 | 字段名称 | 一句话定义 | 列表 | 查询 | 新增/编辑 | 详情 | 备注 |
| :--- | :--- | :--- | :---: | :---: | :---: | :---: | :--- |
| 主对象 | [字段] | [定义] | 是 / 否 | 是 / 否 | 是 / 否 / `-` | 是 / 否 | [来源或说明] |
| 只读明细 | [字段] | [定义] | 是 / 否 | 是 / 否 | `-` | 是 / 否 | [说明] |

> 定稿时删除本整节，再填写下方完整章节。

---

## 1. 模块与数据边界

### 1.1 模块定位

| 项目 | 内容 |
| :--- | :--- |
| 模块名称 | [模块名] |
| 业务归属 | [中心/模块] |
| 模块类型 | 外部只读资料 / 纯查询 / 轻量配置 / 极简维护 |
| 维护形态 | 独立页（标准主数据） / 工作台+弹窗（轻量配置） |
| 核心用途 | [用户为什么需要查看或维护这些数据] |

### 1.2 数据来源与维护边界

| 维度 | 说明 |
| :--- | :--- |
| 数据来源 | 外部同步 / ERP本地配置 / 人工维护 / 其他 |
| ERP是否数据源 | 是 / 否 / 部分字段是 |
| 外部系统 | [系统名称；无则填`-`] |
| 同步方式 | 实时 / 定时 / 手动同步 / 不适用 |
| 新增编辑 | 支持 / 不支持 |
| 启用禁用 | 支持 / 不支持 |
| 导入导出 | [范围] |
| 子表/明细 | 无 / 只读明细 |
| 唯一键 | [编码、外部ID或组合键] |

若存在可维护子表、上下游回写、跨系统执行或多维状态，停止使用本模板，改用标准型字段清单。

### 1.3 关键约束

| 约束ID | 约束 | 影响字段/记录 |
| :--- | :--- | :--- |
| D01 | [唯一、防重、只读、引用或启停约束] | [范围] |

---

## 2. 字段维度说明

| 维度 | 允许写法 |
| :--- | :--- |
| 字段名称 | 中文业务名称，不在PM文档定义数据库字段名 |
| 字段来源 | 用户填写 / 接口传递 / 系统生成 / 系统计算 / 系统带出并存储 / 系统自动关联 |
| 取值说明 | 枚举引用、主数据来源、格式或范围；无特殊口径填`-` |
| 必填性 | 必填 / 条件必填（写明条件）/ 选填 / 不适用 |
| 新增页/编辑页 | 可编辑 / 只读 / 不展示 / 不适用；轻量配置时含义为「弹窗可编辑/只读/不展示」 |
| 列表展示/详情展示 | 是 / 否；轻量配置时「详情展示」=查看弹窗是否展示 |
| 可筛选 | 是（注明查询方式）/ 否 |
| 可导入/可导出 | 是 / 否 |
| 字段说明 | 一句话业务含义 |
| 备注 | 数据边界和特殊场景；轻量配置建议标注 `查看=弹窗` |

---

## 3. 主对象字段

> 按实际页面分组复制字段表。不存在的分组不生成。

### 3.1 [基本信息/业务属性/补充信息]

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 可筛选 | 详情展示 | 可导入 | 可导出 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :---: | :--- | :---: | :---: | :---: | :--- | :--- |
| [字段] | [来源] | [口径] | [必填性] | [状态] | [状态] | 是 / 否 | 是（查询方式）/ 否 | 是 / 否 | 是 / 否 | 是 / 否 | [定义] | [说明] |

### 3.2 制单/同步/系统信息（按需）

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 可筛选 | 详情展示 | 可导入 | 可导出 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :---: | :--- | :---: | :---: | :---: | :--- | :--- |
| [创建人/同步时间等] | 系统生成 / 接口传递 | [格式] | 不适用 | 不展示 | 不展示 | 是 / 否 | 是（查询方式）/ 否 | 是 / 否 | 否 | 是 / 否 | [定义] | 页面展示时固定置底 |
| [仅运行字段] | 系统生成 / 计算 | [口径] | 不适用 | 不展示 | 不展示 | 否 | 否 | 否 | 否 | 否 | [定义] | 数据层保留，页面不展示 |

---

## 4. 只读明细字段（如适用）

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 可筛选 | 详情展示 | 可导入 | 可导出 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :---: | :--- | :---: | :---: | :---: | :--- | :--- |
| [字段] | 接口传递 / 系统生成 | [口径] | 不适用 | 不适用 | 不适用 | 否 | 否 | 是 | 否 | 是 / 否 | [定义] | 只读展示 |

需要新增、删除、排序或维护明细行时，改用标准型字段清单。

---

## 5. 枚举值

### 5.1 [枚举字段]

| 枚举值 | 含义 | 是否默认 | 适用条件 | 备注 |
| :--- | :--- | :---: | :--- | :--- |
| [值] | [含义] | 是 / 否 | [条件] | [说明] |

---

## 6. 输入与校验约束（如支持新增/编辑）

| 字段 | 适用动作 | 长度/字符 | 格式/范围 | 条件必填 | 唯一/重复 | 只读/引用限制 | 错误提示建议 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| [字段] | 新增 / 编辑 / 导入 | [约束] | [约束] | [规则] | [范围] | [限制] | [文案] |

纯只读模块删除本节。校验时机和错误展示统一引用全局表单校验规范，不在字段清单重复定义。

---

## 7. 待确认事项

| 编号 | 问题 | 影响字段/记录 | 当前建议 | 状态 |
| :--- | :--- | :--- | :--- | :--- |
| Q01 | [问题] | [范围] | [建议及理由] | 待确认 |

---

## Changelog

| 日期 | 版本 | 变更内容 | 影响范围 |
| :--- | :--- | :--- | :--- |
| YYYY-MM-DD | V1.0 | 定稿：删除粗纲简表，展开完整字段定义 | [范围] |
