# [模块名]字段清单

> **文档定位**：[模块名]字段定义的唯一真相源（SST）。业务PRD和Demo PRD只引用字段名称，不重复维护枚举、必填性和字段口径。
> **适用类型**：完整主数据维护、带可维护子表、业务单据、状态流转、上下游联动或跨系统执行模块。
> **成熟度**：粗纲 / 定稿
> **版本**：V0.x（粗纲）/ V1.0+（定稿）
> **更新时间**：YYYY-MM-DD

---

## 0. 成熟度规则

| 成熟度 | 保留内容 | 能否作为完整SST | 下一步 |
| :--- | :--- | :---: | :--- |
| 粗纲 | 文首信息 + 粗纲简表 + 必要的对象结构说明 | 否 | 支撑业务PRD和页面骨架 |
| 定稿 | 概述 + 完整字段表 + 枚举、计算、校验、导入约束和Changelog | 是 | 生成Demo PRD和前端原型 |

升级定稿时必须删除整节“粗纲简表”，不得在同一文件长期维护两套字段表。历史粗纲通过Git查看。

---

## 粗纲简表（仅粗纲保留）

| 结构 | 字段名称 | 一句话定义 | 列表 | 查询 | 新增/编辑 | 详情 | 备注 |
| :--- | :--- | :--- | :---: | :---: | :---: | :---: | :--- |
| 单据头/主对象 | [字段] | [定义] | 是 / 否 | 是 / 否 | 是 / 否 / `-` | 是 / 否 | [来源或状态] |
| 单据体/子表 | [字段] | [定义] | 是 / 否 | 是 / 否 | 是 / 否 / `-` | 是 / 否 | [说明] |

> 定稿时删除本整节，再填写下方完整章节。

---

## 1. 模块与数据结构

### 1.1 模块定位

| 项目 | 内容 |
| :--- | :--- |
| 模块名称 | [模块名] |
| 业务归属 | [中心/模块] |
| 模块类型 | 主数据 / 配置 / 业务单据 / 执行结果 / 其他 |
| 核心对象 | [本清单定义的业务对象] |
| 核心职责 | [对象解决什么问题] |

### 1.2 对象结构

| 结构 | 对象 | 与主对象关系 | 主键/唯一标识 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| 主表/单据头 | [对象] | 主对象 | [标识] | [说明] |
| 子表/单据体 | [对象] | 1:N / 1:1 | [标识] | [说明] |
| 执行/来源明细 | [对象] | [关系] | [标识] | [说明] |

### 1.3 数据来源与维护边界

| 维度 | 说明 |
| :--- | :--- |
| 数据来源 | ERP维护 / 外部同步 / 上游生成 / 下游回写 / 系统计算 |
| ERP是否数据源 | 是 / 否 / 部分字段是 |
| 新增编辑 | 支持 / 不支持 / 按状态支持 |
| 子表 | 无 / 只读 / 可维护 |
| 导入导出 | [范围] |
| 状态与执行 | [状态维度、上下游和跨系统能力] |
| 关键唯一键 | [字段或组合键] |
| 历史兼容 | [旧数据空值、默认值或迁移口径] |

### 1.4 关键数据约束

| 约束ID | 约束 | 影响对象 |
| :--- | :--- | :--- |
| D01 | [唯一、防重、引用、行数、数量守恒等约束] | [头/行/上下游] |

---

## 2. 字段维度说明

| 维度 | 允许写法 |
| :--- | :--- |
| 字段名称 | 中文业务名称，不在PM文档定义数据库字段名 |
| 字段来源 | 用户填写 / 接口传递 / 系统生成 / 系统计算 / 系统带出并存储 / 系统自动关联 / 下游回写 |
| 取值说明 | 枚举引用、主数据来源、格式、数值范围、日期精度；无特殊口径填`-` |
| 必填性 | 必填 / 条件必填（写明条件）/ 选填 / 不适用 |
| 新增页/编辑页 | 可编辑 / 只读 / 不展示 / 不适用 |
| 列表展示/详情展示 | 是 / 否 |
| 可筛选 | 是（注明查询方式）/ 否 |
| 可导入/可导出 | 是 / 否 |
| 字段说明 | 一句话业务含义 |
| 备注 | 状态边界、版本规划和特殊场景 |

系统生成、计算、带出和回写写在“字段来源”，页面只读写在新增页/编辑页，不得把“系统自动”写成必填性。

---

## 3. 主表/单据头字段

> 按实际分组复制字段表。常见分组为基本信息、业务属性、流程信息、上下游/执行信息、制单信息。不存在的分组不生成。

### 3.1 [分组名称]

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 可筛选 | 详情展示 | 可导入 | 可导出 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :---: | :--- | :---: | :---: | :---: | :--- | :--- |
| [字段] | [来源] | [口径] | [必填性] | [状态] | [状态] | 是 / 否 | 是（查询方式）/ 否 | 是 / 否 | 是 / 否 | 是 / 否 | [定义] | [说明] |

### 3.2 制单/系统信息（按需）

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 可筛选 | 详情展示 | 可导入 | 可导出 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :---: | :--- | :---: | :---: | :---: | :--- | :--- |
| [创建人/创建时间等] | 系统生成 | [格式] | 不适用 | 不展示 | 不展示 | 是 / 否 | 是（查询方式）/ 否 | 是 / 否 | 否 | 是 / 否 | [定义] | 制单信息置底 |
| [仅运行字段] | 系统生成 / 计算 | [口径] | 不适用 | 不展示 | 不展示 | 否 | 否 | 否 | 否 | 否 | [定义] | 数据层保留，页面不展示 |

---

## 4. 子表/明细字段（如适用）

### 4.1 [子表名称]

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 可筛选 | 详情展示 | 可导入 | 可导出 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :---: | :--- | :---: | :---: | :---: | :--- | :--- |
| [字段] | [来源] | [范围/单位/枚举] | [必填性] | [状态] | [状态] | 是 / 否 | 是（查询方式）/ 否 | 是 / 否 | 是 / 否 | 是 / 否 | [定义] | [行状态或来源] |

### 4.2 子表规则

| 规则类型 | 规则 |
| :--- | :--- |
| 行数 | [最少/最多；业务单据商品明细默认最多100行] |
| 唯一/重复 | [同一主记录下的组合键] |
| 新增删除 | [允许条件和引用/执行限制] |
| 行状态 | [产生方式及对编辑、下推、关闭的影响] |
| 数量/金额 | [0、负数、小数位和汇总口径] |
| 上下游执行 | [已执行、可执行、失败和回写口径] |

---

## 5. 枚举值

> 每个枚举字段复制一节。Demo PRD只引用字段名称，不重复枚举全集。

### 5.1 [枚举字段]

| 枚举值 | 含义 | 是否默认 | 适用条件 | 备注 |
| :--- | :--- | :---: | :--- | :--- |
| [值] | [含义] | 是 / 否 | [条件] | [说明] |

---

## 6. 计算、联动与回写口径（如适用）

| 规则ID | 触发 | 输入字段/来源 | 计算或联动规则 | 影响字段 | 重算/覆盖方式 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| C01 | [字段变化/动作/回写] | [字段] | [公式或规则] | [字段] | 增量 / 覆盖 / 汇总重算 |

涉及数量和金额时，应写清精度、舍入、尾差、头行汇总及历史结果是否重算。

---

## 7. 输入与校验约束

### 7.1 主表字段

| 字段 | 适用动作 | 长度/字符 | 格式/范围 | 条件必填 | 唯一/重复 | 引用/状态限制 | 错误提示建议 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| [字段] | 新增 / 编辑 / 导入 | [约束] | [约束] | [规则] | [范围] | [限制] | [文案] |

### 7.2 子表字段（如适用）

| 子表 | 字段 | 适用动作 | 格式/范围 | 行级必填 | 行级重复 | 状态/数量限制 | 错误定位 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| [子表] | [字段] | 新增行 / 编辑行 / 导入 | [约束] | [规则] | [规则] | [限制] | 第{行号}行[字段名称] |

校验时机和错误展示不在字段清单重复定义，统一引用全局表单校验规范。

---

## 8. 导入约束（如适用）

| 校验项 | 规则 | 失败结果 |
| :--- | :--- | :--- |
| 字段范围 | 只允许“可导入=是”的字段 | [阻断或标记失败行] |
| 枚举 | 必须命中本文件枚举 | [结果] |
| 唯一与引用 | 与新增编辑使用同一业务口径 | [结果] |
| 子表/明细 | 逐行执行本文件约束 | [结果] |
| 部分成功 | 以业务PRD为准 | [成功数、失败数和失败明细] |

---

## 9. 待确认事项

| 编号 | 问题 | 影响字段/对象 | 当前建议 | 状态 |
| :--- | :--- | :--- | :--- | :--- |
| Q01 | [问题] | [字段/头行/上下游] | [建议及理由] | 待确认 |

---

## Changelog

| 日期 | 版本 | 变更内容 | 影响范围 |
| :--- | :--- | :--- | :--- |
| YYYY-MM-DD | V1.0 | 定稿：删除粗纲简表，展开完整字段定义 | [范围] |
