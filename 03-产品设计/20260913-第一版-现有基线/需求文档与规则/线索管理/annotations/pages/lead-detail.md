<!-- anno:start id=1 type=page page=线索管理/线索详情 target=lead-detail-page-header -->
## 需求描述：【线索详情页面】

> 线索详情页面，查看单条线索的完整状态、基本信息、评分、跟进记录与可执行操作。

### 页面内容
#### 页面定位
- 页面名称：线索管理/线索详情，用于查看线索完整资料、AI评分、跟进时间线并执行状态动作。
- 页面骨架：顶部标识区、状态区、AI评分卡片、基本信息卡片、跟进记录时间线和固定底部操作栏。

### 交互说明
#### 页面进入
- 页面进入后展示当前线索的最新状态和关联信息。
- 详情页只通过业务动作按钮触发状态流转，不提供状态字段直接编辑。
- 不同状态显示不同的底部操作，用户只能执行当前状态和权限允许的动作。

### 业务规则
#### 生命周期边界
- 线索状态包括草稿（DRAFT）、待分配（PENDING_ASSIGN）、已分配（ASSIGNED）、跟进中（FOLLOWING）、已转客户（CONVERTED）和已作废（ABANDONED）。
- 已转客户是不可逆的归档只读终态；客户正式档案的权威来源是 ERP。
- 已作废是否可恢复、是否可认领由作废类型和原负责人保护规则决定。

### 字段说明
| 字段所属分组 | 字段名称 | 字段类型 | 字段来源 | 字段说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 可筛选 | 详情展示 | 取值说明 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 基础信息 | 线索编号 | 文本 | 系统生成 | 线索唯一标识 | 不适用 | 不展示 | 只读 | 是(160px) | 是(Input) | 是 | LEAD{YYYYMMDD}-{4位序号} | 系统自动生成业务唯一标识 |
| 基础信息 | 线索来源 | 单选 | 用户填写 | 线索获取渠道 | 必填 | Select | Select | 是(120px) | 是(Select) | 是 | Select: ONLINE=官网, ACTIVITY=线下活动, EXHIBITION=展会, REFERRAL=转介绍, IMPORT=批量导入, OTHER=其他 | AI评分维度之一(权重15%) |
| 基础信息 | 公司名称 | 文本 | 用户填写 | 线索所属公司 | 必填 | Input | Input | 是(180px) | 是(Input) | 是 | 文本, 上限100字符 | - |
| 基础信息 | 联系人 | 文本 | 用户填写 | 联系人姓名 | 选填 | Input | Input | 是(100px) | 否 | 是 | 文本, 上限50字符 | - |
| 基础信息 | 手机号 | 文本 | 用户填写 | 联系人手机号 | 条件必填 | Input | Input | 是(140px) | 是(Input) | 是 | 11位数字, 唯一校验 | 与邮箱至少填一个；全局去重字段 |
| 基础信息 | 邮箱 | 文本 | 用户填写 | 联系人邮箱 | 条件必填 | Input | Input | 是(180px) | 是(Input) | 是 | 邮箱格式校验, 唯一校验 | 与手机号至少填一个；全局去重字段 |
| 基础信息 | 职位 | 文本 | 用户填写 | 联系人职位 | 选填 | Input | Input | 否 | 否 | 是 | 文本, 上限50字符 | - |
| 基础信息 | 所属行业 | 单选 | 用户填写 | 所在行业 | 选填 | Select | Select | 是(120px) | 是(Select) | 是 | Select: MANUFACTURING=制造业, RETAIL=零售, HEALTHCARE=医疗, FINANCE=金融, IT=信息技术, OTHER=其他 | AI评分维度之一(权重20%) |
| 基础信息 | 所在地区 | 级联选择 | 用户填写 | 所在省市 | 选填 | Cascader | Cascader | 是(120px) | 是(Cascader) | 是 | 省-市, 级联选择 | - |
| 基础信息 | 线索备注 | 多行文本 | 用户填写 | 备注信息 | 选填 | Textarea | Textarea | 否 | 否 | 是 | Textarea, 上限500字符 | - |
| 基础信息 | AI评分 | 整数 | 系统计算 | AI评分模型输出 | 不适用 | 不展示 | 不展示 | 是(100px) | 是(Range) | 是 | 0-100 整数；筛选时最小值≤最大值且均在0-100范围内，超限或最小值大于最大值时阻断并提示“最小评分不能大于最大评分” | 创建时触发；查询区按边界校验后执行筛选 |
| 基础信息 | 线索状态 | 单选 | 系统生成 | 当前状态 | 不适用 | 不展示 | 只读 | 是(120px) | 是(Select) | 是 | DRAFT=草稿, PENDING_ASSIGN=待分配, ASSIGNED=已分配, FOLLOWING=跟进中, CONVERTED=已转客户, ABANDONED=已作废 | 状态机驱动；状态变更必须通过动作按钮触发 |
| 分配与跟进 | 归属人 | 文本 | 系统生成 | 当前负责人 | 不适用 | 不展示 | 只读 | 是(100px) | 是(Select) | 是 | 被分配的销售姓名 | 分配/认领或管理作废时写入/清空；一期自动回收时清空；主动放弃时保留，供原负责人立即撤销放弃 |
| 分配与跟进 | 分配时间 | 日期时间 | 系统生成 | 最近一次分配时间 | 不适用 | 不展示 | 只读 | 是(160px) | 否 | 是 | YYYY-MM-DD HH:mm:ss | 主动放弃时清空；原负责人撤销放弃或其他销售重新认领时重置为当前时间 |
| 分配与跟进 | 最近跟进时间 | 日期时间 | 系统生成 | 最近一次添加跟进记录的时间 | 不适用 | 不展示 | 只读 | 是(160px) | 否 | 是 | YYYY-MM-DD HH:mm:ss | 当前列表回收仅针对已分配且无跟进记录的线索，以分配时间判断；放弃操作当前也会更新该字段 |
| 分配与跟进 | 放弃原因 | 多行文本 | 用户填写 | 放弃时填写的理由 | 条件必填 | 不展示 | 不展示 | 否 | 否 | 是 | Textarea，至少15字 | 主动放弃或作废时必填；少于15字时阻断并提示“放弃原因至少15字” |
| 系统字段 | 创建时间 | 日期时间 | 系统生成 | 录入时间 | 不适用 | 不展示 | 只读 | 是(160px) | 是(DateRange) | 是 | YYYY-MM-DD HH:mm:ss | 列表默认按此倒序；支持今天/本周/本月快捷筛选和自定义日期区间 |


### 待确认
<!-- anno:end id=1 -->

<!-- anno:start id=2 type=interaction page=线索管理/线索详情 target=lead-detail-status-owner -->
## 需求描述：【状态与归属信息】

> 状态与归属区，展示线索当前状态标签、编号、归属人和分配时间。

### 页面内容
#### 状态区展示
- 状态区展示当前状态标签、线索编号、归属人和分配时间。
- 状态标签使用与列表一致的中文业务颜色，英文状态值仅作为规则对照。

### 交互说明
#### 状态变化
- 状态不通过下拉框或文本直接修改，只能由提交、认领、添加跟进、转客户、主动放弃和作废等动作触发。
- 认领成功后更新归属人和分配时间；主动放弃保留原归属人但清空分配时间。
- 状态变化后刷新详情信息及可用操作按钮。

### 业务规则
#### 状态含义
- 草稿（DRAFT）：手动录入暂存，允许继续编辑。
- 待分配（PENDING_ASSIGN）：等待分配或处于培育池的统一阶段状态。
- 已分配（ASSIGNED）：已有归属销售但尚未完成首次有效跟进。
- 跟进中（FOLLOWING）：已完成首次有效跟进，可继续跟进或申请转客户。
- 已转客户（CONVERTED）：ERP 建档成功后的不可逆只读终态。
- 已作废（ABANDONED）：作废或主动放弃后的状态，是否进入可恢复公海取决于作废类型。

### 字段说明
| 字段 | 必填/可编辑 | 核心约束 |
| --- | --- | --- |
| 线索编号 | 系统生成；只读 | `LEAD{YYYYMMDD}-{4位序号}` |
| 线索状态 | 系统生成；只读 | 由状态机和动作按钮驱动 |
| 归属人 | 系统生成；只读 | 分配/认领时写入；超时回收时清空；主动放弃时保留 |
| 分配时间 | 系统生成；只读 | 分配或认领时写入；主动放弃时清空 |

### 待确认
<!-- anno:end id=2 -->

<!-- anno:start id=3 type=rule page=线索管理/线索详情 target=lead-detail-ai-score -->
## 需求描述：【AI评分与评分维度】

> AI评分卡片，展示评分依据、各维度权重与流向规则，帮助销售理解预测结果。

### 页面内容
#### 评分卡片
- 卡片展示综合 AI 评分，分值为0-100的整数，并按高、中、低区间使用绿色、黄色、红色区分。
- 展开后展示评分维度明细：来源、行业、首次响应、历史转化和活跃度。
- 页面可展示最近一次评分时间；评分字段由系统计算，用户不可手工修改。

### 交互说明
#### 展开行为
- 点击“AI评分权重拆解”展开或收起维度明细，不改变线索状态和其他基本信息。
- 评分在新建提交或批量导入成功后生成，详情页负责展示最新结果。

### 业务规则
#### 评分与流向
- 评分权重：线索来源15%、所属行业20%、首次响应速度25%、行业历史转化概率25%、活跃度基准评估15%。
- 评分≥80分：按路由规则自动分配销售。
- 评分50-79分：进入待分配池（ASSIGN_POOL）。
- 评分<50分：进入培育池（NURTURE_POOL），状态仍为待分配（PENDING_ASSIGN）。
- 评分接口失败或超时：按50分降级，进入待分配池并记录失败日志；重试不得打断已经进入人工处理的状态。

### 字段说明
| 字段 | 必填/可编辑 | 核心约束 |
| --- | --- | --- |
| AI评分 | 系统自动；只读 | 0-100整数；≥80绿色、50-79黄色、<50红色 |
| 评分时间 | 系统自动；只读 | 最近一次评分完成时间 |
| 评分维度明细 | 系统计算；只读 | 展示五项静态加权维度 |

### 待确认
<!-- anno:end id=3 -->

<!-- anno:start id=4 type=field page=线索管理/线索详情 target=lead-detail-basic-info -->
## 需求描述：【线索基本信息】

> 线索基本信息卡片，只读展示线索来源、公司、联系人、手机号、邮箱、行业、地区等信息。

### 页面内容
#### 信息展示
- 基本信息卡片使用四列 Key-Value 布局，只读展示线索来源、公司名称、联系人、手机号、邮箱、职位、所属行业、所在地区和线索备注。
- 已作废线索额外展示放弃/作废原因。
- 系统字段可在详情中展示创建人、创建时间、最后修改人、最后修改时间和转化时间等信息。

### 交互说明
#### 只读展示
- 详情页不直接编辑基本字段；草稿需要通过编辑入口进入新增编辑页维护。
- 转客户成功后基本信息和系统字段进入只读归档状态。

### 业务规则
#### 数据边界
- 详情页展示 CRM 持有的线索信息和客户快照关联信息，不替代 ERP 客户主数据。
- 转客户时写入客户快照并与 ERP 建档结果保持一致；ERP 失败时不得提前将线索改为已转客户。

### 字段说明
| 字段所属分组 | 字段名称 | 字段类型 | 字段来源 | 字段说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 可筛选 | 详情展示 | 取值说明 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 基础信息 | 线索编号 | 文本 | 系统生成 | 线索唯一标识 | 不适用 | 不展示 | 只读 | 是(160px) | 是(Input) | 是 | LEAD{YYYYMMDD}-{4位序号} | 系统自动生成业务唯一标识 |
| 基础信息 | 线索来源 | 单选 | 用户填写 | 线索获取渠道 | 必填 | Select | Select | 是(120px) | 是(Select) | 是 | Select: ONLINE=官网, ACTIVITY=线下活动, EXHIBITION=展会, REFERRAL=转介绍, IMPORT=批量导入, OTHER=其他 | AI评分维度之一(权重15%) |
| 基础信息 | 公司名称 | 文本 | 用户填写 | 线索所属公司 | 必填 | Input | Input | 是(180px) | 是(Input) | 是 | 文本, 上限100字符 | - |
| 基础信息 | 联系人 | 文本 | 用户填写 | 联系人姓名 | 选填 | Input | Input | 是(100px) | 否 | 是 | 文本, 上限50字符 | - |
| 基础信息 | 手机号 | 文本 | 用户填写 | 联系人手机号 | 条件必填 | Input | Input | 是(140px) | 是(Input) | 是 | 11位数字, 唯一校验 | 与邮箱至少填一个；全局去重字段 |
| 基础信息 | 邮箱 | 文本 | 用户填写 | 联系人邮箱 | 条件必填 | Input | Input | 是(180px) | 是(Input) | 是 | 邮箱格式校验, 唯一校验 | 与手机号至少填一个；全局去重字段 |
| 基础信息 | 职位 | 文本 | 用户填写 | 联系人职位 | 选填 | Input | Input | 否 | 否 | 是 | 文本, 上限50字符 | - |
| 基础信息 | 所属行业 | 单选 | 用户填写 | 所在行业 | 选填 | Select | Select | 是(120px) | 是(Select) | 是 | Select: MANUFACTURING=制造业, RETAIL=零售, HEALTHCARE=医疗, FINANCE=金融, IT=信息技术, OTHER=其他 | AI评分维度之一(权重20%) |
| 基础信息 | 所在地区 | 级联选择 | 用户填写 | 所在省市 | 选填 | Cascader | Cascader | 是(120px) | 是(Cascader) | 是 | 省-市, 级联选择 | - |
| 基础信息 | 线索备注 | 多行文本 | 用户填写 | 备注信息 | 选填 | Textarea | Textarea | 否 | 否 | 是 | Textarea, 上限500字符 | - |
| 基础信息 | AI评分 | 整数 | 系统计算 | AI评分模型输出 | 不适用 | 不展示 | 不展示 | 是(100px) | 是(Range) | 是 | 0-100 整数；筛选时最小值≤最大值且均在0-100范围内，超限或最小值大于最大值时阻断并提示“最小评分不能大于最大评分” | 创建时触发；查询区按边界校验后执行筛选 |
| 基础信息 | 线索状态 | 单选 | 系统生成 | 当前状态 | 不适用 | 不展示 | 只读 | 是(120px) | 是(Select) | 是 | DRAFT=草稿, PENDING_ASSIGN=待分配, ASSIGNED=已分配, FOLLOWING=跟进中, CONVERTED=已转客户, ABANDONED=已作废 | 状态机驱动；状态变更必须通过动作按钮触发 |
| 分配与跟进 | 归属人 | 文本 | 系统生成 | 当前负责人 | 不适用 | 不展示 | 只读 | 是(100px) | 是(Select) | 是 | 被分配的销售姓名 | 分配/认领或管理作废时写入/清空；一期自动回收时清空；主动放弃时保留，供原负责人立即撤销放弃 |
| 分配与跟进 | 分配时间 | 日期时间 | 系统生成 | 最近一次分配时间 | 不适用 | 不展示 | 只读 | 是(160px) | 否 | 是 | YYYY-MM-DD HH:mm:ss | 主动放弃时清空；原负责人撤销放弃或其他销售重新认领时重置为当前时间 |
| 分配与跟进 | 最近跟进时间 | 日期时间 | 系统生成 | 最近一次添加跟进记录的时间 | 不适用 | 不展示 | 只读 | 是(160px) | 否 | 是 | YYYY-MM-DD HH:mm:ss | 当前列表回收仅针对已分配且无跟进记录的线索，以分配时间判断；放弃操作当前也会更新该字段 |
| 分配与跟进 | 放弃原因 | 多行文本 | 用户填写 | 放弃时填写的理由 | 条件必填 | 不展示 | 不展示 | 否 | 否 | 是 | Textarea，至少15字 | 主动放弃或作废时必填；少于15字时阻断并提示“放弃原因至少15字” |
| 系统字段 | 创建时间 | 日期时间 | 系统生成 | 录入时间 | 不适用 | 不展示 | 只读 | 是(160px) | 是(DateRange) | 是 | YYYY-MM-DD HH:mm:ss | 列表默认按此倒序；支持今天/本周/本月快捷筛选和自定义日期区间 |


### 待确认
<!-- anno:end id=4 -->

<!-- anno:start id=5 type=interaction page=线索管理/线索详情 target=lead-detail-follow-up -->
## 需求描述：【跟进记录时间线】

> 跟进记录时间线，按时间倒序展示跟进历史，支持添加新跟进记录。

### 页面内容
#### 时间线展示
- 跟进记录按时间倒序排列，展示时间、跟进人、跟进方式、跟进内容和下次跟进计划。
- 跟进内容较长时折叠展示，用户可展开查看全部内容。
- 没有记录时展示“暂无跟进记录”。

### 交互说明
#### 添加跟进
- 已分配或跟进中的线索显示“添加跟进”入口；点击后打开跟进记录表单。
- 首次有效跟进后，状态由已分配（ASSIGNED）变为跟进中（FOLLOWING）。
- 跟进中追加记录后仍保持跟进中，并更新最近跟进时间。
- 保存成功后刷新时间线并清空本次录入内容；保存失败保持当前状态并提示错误。

### 业务规则
#### 跟进留痕
- 跟进记录由当前归属销售录入，形成线索生命周期的可追溯记录。
- 已分配且超过48小时没有任何跟进记录时，列表数据更新可触发一期超时回收；跟进中线索不适用该一期回收规则。
- 转客户、主动放弃等动作产生的系统记录也应进入线索跟进历史。

### 字段说明
| 字段 | 必填/可编辑 | 核心约束 |
| --- | --- | --- |
| 跟进时间 | 系统生成；只读 | 保存记录时写入当前时间 |
| 跟进人 | 系统生成；只读 | 当前操作销售姓名 |
| 跟进方式 | 必填；提交时可编辑 | 电话、拜访、邮件 |
| 跟进内容 | 必填；提交时可编辑 | 记录客户最新沟通、痛点或诉求 |
| 下次跟进计划 | 选填；提交时可编辑 | 记录下一步销售安排 |

### 待确认
<!-- anno:end id=5 -->

<!-- anno:start id=6 type=interaction page=线索管理/线索详情 target=lead-detail-action-bar -->
## 需求描述：【状态操作栏与动作入口】

> 状态操作栏，按当前状态和权限动态展示认领、跟进、转客户、放弃、作废等动作入口。

### 页面内容
#### 操作布局
- 固定底部操作栏将“返回列表”放在左侧次要位置，主操作按当前线索状态动态显示在右侧。
- 操作入口只展示当前状态、业务池、作废类型和用户权限共同允许的动作。

### 交互说明
#### 状态动作
- 草稿（DRAFT）：返回、编辑、提交、作废草稿。
- 待分配（PENDING_ASSIGN）：返回、认领（仅可认领的待分配池线索）。
- 已分配（ASSIGNED）：返回、添加跟进、放弃。
- 跟进中（FOLLOWING）：返回、添加跟进、转客户、放弃。
- 已转客户（CONVERTED）：返回、查看关联 ERP 客户。
- 已作废（ABANDONED）：默认返回；销售主动放弃记录按公海规则提供原负责人撤销或其他销售认领。

### 业务规则
#### 权限与动作边界
- 销售只能操作本人归属线索或满足公海可见条件的主动放弃线索；主管和管理员按团队或全局权限执行管理动作。
- “认领”“添加跟进”“转客户”“放弃”“作废”均为状态变更动作，必须通过动作按钮触发。
- 已转客户为只读终态，不展示编辑、跟进、放弃或再次转客户入口。
- 业务动作提交期间应防止重复点击，并在并发状态变化时提示刷新后重试。

### 字段说明
| 状态 | 主操作 |
| --- | --- |
| 草稿（DRAFT） | 编辑、提交、作废草稿 |
| 待分配（PENDING_ASSIGN） | 认领 |
| 已分配（ASSIGNED） | 添加跟进、放弃 |
| 跟进中（FOLLOWING） | 添加跟进、转客户、放弃 |
| 已转客户（CONVERTED） | 查看关联 ERP 客户 |
| 已作废（ABANDONED） | 按作废类型提供恢复/认领规则 |

### 待确认
<!-- anno:end id=6 -->

<!-- anno:start id=7 type=interaction page=线索管理/线索详情 target=lead-detail-follow-up-modal -->
## 需求描述：【添加跟进记录弹窗】

> 添加跟进记录弹窗，选择跟进方式并填写内容，保存后写入时间线并更新最近跟进时间。

### 页面内容
#### 弹窗字段
- 弹窗提供跟进方式、跟进内容和下次跟进计划三个字段。
- 跟进方式和跟进内容为必填；下次跟进计划为选填。

### 交互说明
#### 提交流程
- 点击“添加跟进”打开弹窗；点击取消关闭弹窗并清空本次未提交内容。
- 提交记录后关闭弹窗，刷新时间线并更新最近跟进时间。
- 提交按钮在内容为空时不可用，写入过程中避免重复提交。

### 业务规则
#### 状态影响
- 已分配线索添加第一条有效跟进后变为跟进中（FOLLOWING）。
- 跟进中线索继续追加跟进记录，状态保持跟进中。
- 跟进记录保存失败时不应修改最近跟进时间，需提示用户重试。

### 字段说明
| 字段 | 必填/可编辑 | 核心约束 |
| --- | --- | --- |
| 跟进方式 | 必填；提交时可编辑 | 电话、拜访、邮件 |
| 跟进内容 | 必填；提交时可编辑 | 客户沟通内容、最新痛点或诉求 |
| 下次跟进计划 | 选填；提交时可编辑 | 下一步跟进安排 |

### 待确认
<!-- anno:end id=7 -->

<!-- anno:start id=8 type=interaction page=线索管理/线索详情 target=lead-detail-abandon-modal -->
## 需求描述：【主动放弃线索弹窗】

> 主动放弃线索弹窗，说明放弃后果并填写至少15字原因，确认后进入已作废进公海。

### 页面内容
#### 弹窗字段
- 弹窗标题为“确认放弃线索”，说明放弃后线索进入公海。
- 放弃原因使用多行文本输入，确认按钮在原因为空或不符合长度规则时阻断。

### 交互说明
#### 确认流程
- 点击“放弃线索”打开二次确认弹窗；点击取消关闭弹窗并清空未提交原因。
- 点击“确认放弃”后写入放弃原因、更新状态并追加放弃跟进记录。
- 操作成功后关闭弹窗并刷新详情及底部操作栏。

### 业务规则
#### 放弃与公海
- 销售主动放弃仅允许当前归属销售发起，状态变更为已作废（ABANDONED）。
- 放弃原因至少15字；少于15字时阻断并提示“放弃原因至少15字”。
- 主动放弃保留原归属人、清空分配时间；原负责人可立即撤销放弃并恢复为已分配（ASSIGNED）。
- 其他销售受7天防撞墙保护，超过7天后才可见并认领；管理作废不开放销售认领。

### 字段说明
| 字段 | 必填/可编辑 | 核心约束 |
| --- | --- | --- |
| 放弃原因 | 放弃时必填；确认前可编辑 | 多行文本，至少15字 |
| 作废类型 | 系统生成；只读 | 销售主动放弃（VOLUNTARY_ABANDON） |

### 待确认
<!-- anno:end id=8 -->

<!-- anno:start id=9 type=interaction page=线索管理/线索详情 target=lead-detail-convert-modal -->
## 需求描述：【转客户确认与 ERP 建档】

> 转客户确认弹窗，二次确认并同步ERP建档，成功后线索变为已转客户终态。

### 页面内容
#### 确认弹窗
- 弹窗说明转客户后将同步 ERP 建档，线索变为只读且不可逆。
- 提供取消和确认转客户两个动作。

### 交互说明
#### 转化流程
- 仅跟进中线索显示“转客户”入口；点击后先弹出二次确认。
- 确认后向 ERP 发起客户建档，并等待建档结果。
- ERP 建档成功后关闭弹窗，更新状态、转化时间和客户快照，并提供查看关联 ERP 客户入口。
- ERP 建档失败时关闭或保留弹窗均不得提前改变线索状态，应展示具体失败原因并允许按业务规则重试。

### 业务规则
#### 强一致性
- 只有 ERP 返回建档成功和客户编码后，CRM 才能将线索改为已转客户（CONVERTED）。
- 转化成功后生成客户快照，线索进入不可逆只读终态，不允许再次转化或修改基础信息。
- ERP 失败时保持跟进中（FOLLOWING），不写入转化时间，不生成客户快照。
- 转客户请求需要幂等，重试不能重复创建客户或重复生成客户快照。

### 字段说明
| 字段/结果 | 说明 |
| --- | --- |
| 转化时间 | ERP 建档成功后由系统写入 |
| 客户编码 | ERP 建档成功后返回并用于关联客户 |
| 客户快照 | CRM 记录公司名称、联系人、手机号等基础信息的转化快照 |
| 线索状态 | 成功后为已转客户（CONVERTED），失败保持跟进中（FOLLOWING） |

### 待确认
<!-- anno:end id=9 -->

<!-- anno:start id=10 type=rule page=线索管理/线索详情 target=lead-detail-draft-void-modal -->
## 需求描述：【草稿作废动作】

> 草稿作废动作，草稿状态可作废，作废后保留记录、替代物理删除，不进入公海认领范围。

### 页面内容
#### 确认弹窗
- 草稿状态提供作废草稿入口，弹窗需要说明作废后保留草稿记录但不可继续编辑。
- 确认动作应明确“确认作废”，取消动作不改变当前草稿。

### 交互说明
#### 作废流程
- 点击草稿操作栏中的作废草稿后打开二次确认弹窗。
- 确认后保留线索内容、状态变更记录和操作痕迹，刷新详情并回到列表流程。
- 取消后关闭弹窗，草稿继续保持可编辑。

### 业务规则
#### 作废边界
- 草稿作废由创建人、所属团队销售主管或系统管理员按权限执行。
- 作废草稿结果为已作废（ABANDONED），作废类型为作废草稿（DRAFT_VOID）。
- 草稿作废替代物理删除，记录保留且不进入销售公海认领范围。
- 作废原因按字段清单规则校验，少于15字时阻断并提示“放弃原因至少15字”。

### 字段说明
| 字段/结果 | 说明 |
| --- | --- |
| 放弃原因 | 作废时必填，至少15字 |
| 作废类型 | 作废草稿（DRAFT_VOID），系统生成只读 |
| 线索状态 | 已作废（ABANDONED），保留原草稿内容和操作痕迹 |

### 待确认
- 当前原型按钮文案为“删除”，而 PRD 定义为“作废草稿”；本轮仅补充标注，是否同步统一业务文案待确认。
<!-- anno:end id=10 -->

<!-- anno:start id=11 type=page-global page=线索管理/线索详情 -->
## 需求描述：【本页规则】

> 本页规则，按状态和权限计算可见动作，处理终态只读、防重复提交、并发冲突等页面级行为。

### 页面内容
#### 页面级处理
- 详情页按线索状态展示状态区、基本信息、跟进记录和底部动作，详情数据遵循当前用户数据权限。

### 交互说明
#### 准入与动作
- 页面准入：角色、负责人和线索状态共同决定可见动作；无权限动作不展示。
- 终态保护：已转客户线索进入只读状态，普通销售和主管不能修改基础信息。
- 防重复提交：转客户、放弃、添加跟进等写操作提交后按钮锁定，避免重复请求。
- 并发变化：竞争操作失败时提示数据已被其他用户处理，并刷新详情状态。

### 业务规则
#### 外部联动与失败
- 转客户：ERP 建档成功后才变更为已转客户；ERP 失败时保留原线索状态并提示重试。
- 作废/放弃：动作必须符合当前状态和角色权限，保留对应操作痕迹。

### 字段说明
- 本块不新增字段定义；详情字段沿用本页既有标注和《线索字段清单》。

### 待确认
- 无。
<!-- anno:end id=11 -->
