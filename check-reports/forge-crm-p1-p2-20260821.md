# AI 自检报告

## 项目/任务

- 项目：Forge CRM 前端原型
- 任务：P1/P2 收口返修，补齐前端可做的列表分页、高级筛选、状态动作矩阵和部分客户 360°信息架构。
- 约束：只修改 `front-prototype/src/`；未修改后端、Mock 数据、PRD、context、标注源文件；未 commit、未 push。
- 口径：业绩金额严格按 CRM 自有 WON 金额；真实 ERP 签署、订单同步、服务端并发锁和月末结算继续保持 Mock/未触发/服务端状态。

## 改动文件

- 新增 `front-prototype/src/components/list-pagination.tsx`：共用每页条数、页码、总数和边界控制。
- `front-prototype/src/pages/LeadPool.tsx`：入池日期筛选、分页。
- `front-prototype/src/pages/LeadsList.tsx`：创建日期区间筛选。
- `front-prototype/src/pages/OpportunitiesList.tsx`：成交概率区间、预计成交日期区间、列表分页。
- `front-prototype/src/pages/CustomersList.tsx`：生命周期/同步状态筛选和表格展示。
- `front-prototype/src/pages/CustomerDetail.tsx`：关联拜访 360°区块，保留 CRM/ERP 快照与停用阻断。
- `front-prototype/src/pages/ContractList.tsx`：已作废独立 Tab、合同日期筛选、商机/ERP 联动状态列。
- `front-prototype/src/pages/VisitList.tsx`：拜访方式、负责人、日期、执行异常筛选；执行结果列；计划态编辑入口。
- `front-prototype/src/pages/TargetList.tsx`：月份、状态 Tab、销售/关键词筛选、分页、CSV 导出；金额仍按 CRM WON 归集。

## 审计项收口

| 审计项 | 本轮结果 |
|---|---|
| L-08 线索列表查询/日期 | 增加创建日期区间，原有来源、行业、负责人、评分区间和分页保留。 |
| P-04 公海列表 | 增加入池类型、入池日期区间、评分区间和分页。 |
| O-07 商机列表 | 增加成交概率、预计成交日期区间和列表分页；看板继续按阶段展示，终态不可拖拽。 |
| C-06 客户 360° | 增加生命周期/同步状态字段展示，并在客户详情加入直接关联拜访表。 |
| K-06 合同列表 | 已作废独立查询 Tab，增加日期区间和商机/ERP 联动状态展示。 |
| V-06 拜访列表 | 增加负责人/方式/日期/异常筛选，补充执行结果字段和计划态编辑动作。 |
| T-07 业绩列表 | 增加月份导航、进行中/结算中/异常/已结算 Tab、销售筛选、关键词、分页和 CSV 导出。 |

所有状态变更仍通过动作按钮或领域动作触发；本轮未增加表单直接改状态入口，也未增加主数据删除入口。

## 自检结果

1. `git pull --ff-only`：`Already up to date.`
2. `git diff --check -- src`：通过。
3. `cd front-prototype && npm run build`：通过；Vite 仅提示产物 chunk 大于 500 kB。
4. `npm run lint`：退出码 0；仅既有 UI Fast Refresh 和 `public/annotation-kit/runtime.js` warning。
5. 打包：`/tmp/forge-crm-p1p2-20260821072556.zip`；解压第一层包含 `index.html`，未嵌套 `dist/`。
6. 部署：最终 dist 已 rsync 到 incoming；发布脚本实际消费 zip，随后使用 `bash /root/deploy-all.sh --deploy forge-crm` 发布成功。
7. 线上资源：发布目录已切换到 `index-3mugB-At.js`；`https://pmlophy.com/project/forge-crm/` 返回 HTTP 200。
8. 浏览器回归：
   - 公海存在入池日期筛选和分页。
   - 商机列表从 15 行筛选到成交概率不低于 90 分的 3 行，分页仍存在。
   - 业绩目标存在状态 Tab、分页、导出按钮，页面明确显示 CRM WON 口径；筛选“已结算”可得到空态。
   - 合同存在“已作废”独立 Tab、日期筛选和联动状态列。
   - 拜访存在方式/负责人/异常/日期筛选、执行结果列和计划态“编辑”入口。
   - 各已访问页面没有控制台 error。

## 遗留风险

- 本次浏览器隔离环境的 ERP Mock 客户库为空，因此客户列表展示空态，无法用线上真实客户记录点击进入详情验证拜访区块；空态本身无运行时错误。代码和 build 已验证 `CustomerDetail` 的关联拜访渲染路径。
- 真实 ERP 订单、外部签署事件、服务端并发锁、月末结算任务和失败重试队列仍未接入，本轮没有用前端伪造这些结果。
- 目标页顶部汇总保持当前月份团队总览，不随单个状态 Tab 缩小口径；表格明细会按筛选条件变化。
- 旧浏览器 IndexedDB 若没有新字段，会走兼容默认值；正式联调仍需数据迁移策略。

