# AI 自检报告

## 项目任务

依据《线索 PRD 补全决策定稿_20260809.md》，补全线索主 PRD、Demo 列表页 PRD、字段清单，并同步更新线索列表标注来源，消除已拍板规则的“待确认”空白。

## 决策落实对照

### 主 PRD《线索主PRD.md》

- A2：状态机、5.3 流转表、R10 均明确原负责人可立即撤销放弃，防撞墙仅约束其他销售，其他销售超过 7 天后才可见并认领。
- A3：5.3 主动放弃后保留 `归属人`、清空 `分配时间`；撤销放弃或重新认领时重置分配时间。
- D1：功能范围和 R04 明确一期只回收已分配且无跟进记录的线索；跟进中回收和 10 分钟定时扫描列入二期。
- C1：功能范围加入按当前页签和筛选条件导出 Excel/CSV。
- C4：5.3 与 R07 增加放弃原因至少 15 字校验、不足阻断及提示文案。
- C2：权限矩阵增加批量分配线索归属，仅主管/管理员可用。

### Demo《线索_Demo_列表页.md》

- A1：我的线索明确为当前销售创建或归属的 `DRAFT + ASSIGNED + FOLLOWING`。
- B1：关键词支持逗号/换行多值，匹配公司名称、联系人、手机号和线索编号，任一命中即保留。
- B2：AI评分筛选限制 0-100，最小值不得大于最大值，错误阻断并提示“最小评分不能大于最大评分”。
- B3：创建时间支持今天/本周/本月和自定义日期区间。
- B4：查询区增加显式搜索、展开/收起高级筛选和重置行为。
- C1：工具条增加当前页签+筛选结果导出 Excel/CSV。
- C2：表格增加销售主管批量分配。
- C3：表格增加列宽拖拽和操作列固定。
- C4：放弃弹窗增加至少 15 字校验和阻断提示。

### 字段清单《线索字段清单.md》

- A3：负责人主动放弃时保留；一期自动回收时清空。分配时间主动放弃时清空，撤销/认领时重置。
- B2：AI评分字段补充 0-100 和最小/最大边界校验。
- B3：创建时间改为可按快捷选项和自定义区间筛选。
- 其他补全：增加关键词多值、导出范围、批量分配和表格体验规则。

### 标注同步

- 更新 `annotations/pages/leads-list.md` 的状态页签、查询区、工具条、表格、AI评分、行操作和权限规则。
- 保留六个详情 tab 的 `待确认`结构；已拍板规则不再留在待确认内容中，待确认仍保留为评审输入入口。

## 改动文件清单

- `prd-docs/线索管理/线索主PRD.md`
- `prd-docs/线索管理/线索_Demo_列表页.md`
- `prd-docs/线索管理/线索字段清单.md`
- `prd-docs/线索管理/annotations/pages/leads-list.md`
- `front-prototype/public/annotation-kit/annotation.bundle.json`（重新编译）
- `prd-docs/线索管理/annotations/coverage.md`（重新编译）

## 自检结果

- 决策定稿逐项对照：通过。
- 标注编译：通过。
- `npm run build`：通过。
- `npm run lint`：通过；仅有项目既有静态检查 warning，无 error。
- `git diff --check`：通过。
- 未修改业务 React/TS 源码，未 commit，未 push。
- zip 根目录校验：解压后直接包含 `index.html`、`assets/`、`annotation-kit/`。

## 遗留风险

- Demo 及字段清单描述了导出、批量分配、列宽拖拽等定稿能力；本轮仅补全 PRD 与标注说明，未实现业务 UI 行为。
- Vite 仍提示主 JS chunk 超过 500 kB，不影响本轮构建结果。
