# 指令：线索列表页原型标注修复（0913test）

项目：Forge CRM，Mac 路径：/Users/liulongfei/个人文件/forge-crm/，分支：0913test。
第一步先执行：git pull origin 0913test 拿最新代码。

## 任务
修线索列表页原型标注三处。只改标注内容与锚点，不改业务逻辑。

### 1. 块9「权限与异常边界」不再挂在筛选区
原因：它的锚点 leads-permissions 落在筛选框内，徽章因此贴在筛选区。
做法：
- 编辑 03-产品设计/20260913-第一版-现有基线/需求文档与规则/线索管理/annotations/pages/leads-list.md
- 把块9 改为页面级规则：type 换成 page-global（无元素锚点），徽章不定位到筛选区。
- 在 99-产品原型prototype/src/pages/LeadsList.tsx 删除筛选区内那处 data-anno="leads-permissions"。
- 若 schema/compile/runtime 不支持页面级无锚点块，则补最小支持：页面级块的徽章定位到页面顶部，不挂筛选区。

### 2. 字段说明改为表格
对 leads-list.md 三个块的"字段说明"小节，改成 markdown 表格，表头固定为：
字段 | 展示/输入类型 | 业务说明 | 约束与备注
- 块6「表格字段」9 个字段
- 块8「操作相关字段」5 个字段
- 块9「权限相关字段」3 个字段
"展示/输入类型"和"约束与备注"按字段清单 TSV（03-产品设计/20260913-第一版-现有基线/字段清单/线索模块/线索字段清单（详细稿）.tsv）填写；TSV 未定义就填"-"，不要编造。

### 3. 补线索表格的标注徽章
块6「线索表格字段与分页」的徽章当前未在表格上显示。用浏览器控制台检查它是否渲染、是否被块7/块8 的锚点遮挡，修复到表格区域出现一个清晰可见且独立的标注徽章。

## 限制
只改并提交以下文件：
- annotations/pages/leads-list.md
- LeadsList.tsx（仅锚点相关改动）
- 如需才改 schema/compile/runtime（仅页面级支持）
- 编译产物 annotation.bundle.json
不碰其他文件，不改业务逻辑，不删表格自身的 data-anno。

## 完成后
1. 重编译并跑校验，贴出真实输出：
   cd /Users/liulongfei/个人文件/forge-crm/99-产品原型prototype
   python3 prototype-annotation/scripts/compile_annotations.py <配置或工作空间路径>
   python3 prototype-annotation/scripts/check_annotation_assets.py ./public/annotation-kit
2. git add 相关文件
3. git commit -m "fix(标注): 线索列表页块9改页面级、字段说明改表格、补线索表格徽章（0913test）"
4. git push origin 0913test
5. 把自检报告写到 08-相关资料/check-reports/forge-crm-标注修复-0913test-<日期>.md
6. 执行：curl --noproxy '*' -X POST "https://pmlophy.com/p/jarvis/file/upload" -H "X-Jarvis-User: ai-reports" -F "file=@<报告路径>"

## 汇报内容
- 块9 改造的 diff
- 三处字段表格的 diff
- 块6 徽章修复说明
- compile 与 check 的真实运行输出
- push 结果
