# 合同_用例数据推演

> **版本**：V1.1 | 2026-08-13

## 案例1:正常流程——创建→签署→归档→触发赢单

| 步骤 | 操作 | 数据变化 | 验证点 |
|------|------|---------|--------|
| 1 | 商机到CONTRACT,自动创建合同 | `合同编号=CT20260718-0001` `状态=DRAFT` | |
| 2 | 销售提交签署 | `状态:DRAFT→PENDING_SIGN` | |
| 3 | 签署完成 | `状态:PENDING_SIGN→SIGNED` `签署日期=2026-07-18` | 触发商机→WON |
| 4 | 归档 | `状态:SIGNED→ARCHIVED` | 终态 |

原案例验收数据：合同编号 `CT20260718-0001`；商机编号 `OPP20260718-0001`；客户标识 `CUS-DEMO-0001`；金额 `CNY 100000.00`（含税，税率13%）；事件ID `evt-sign-legacy-0001`；请求ID `ext-req-legacy-0001`；前状态 `NEGOTIATION/DRAFT`，后状态 `WON/ARCHIVED`；ERP返回 `orderNo=ERP-20260718-0001`；结论：正常签署、赢单、归档且仅一份订单。

## 案例2:签署阶段作废

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | PENDING_SIGN,不作废原因留空 | **阻断**:"请输入作废原因" |
| 2 | 填写原因后确认 | `状态→VOIDED` |

原案例验收数据：合同编号 `CT20260718-0002`；商机编号 `OPP20260718-0002`；客户标识 `CUS-DEMO-0002`；金额 `CNY 80000.00`（含税，税率13%）；事件ID `evt-void-legacy-0002`；请求ID `req-void-legacy-0002`；前状态 `PENDING_SIGN/CONTRACT`，后状态 `VOIDED/NEGOTIATION`；ERP返回 `未调用`；结论：空原因阻断，合法原因通过作废、解绑与回退。

## 案例3:已签署后不可作废

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | SIGNED状态,点击作废 | **阻断**:"已签署合同不可作废,如需变更请走变更流程" |

原案例验收数据：合同编号 `CT20260718-0003`；商机编号 `OPP20260718-0003`；客户标识 `CUS-DEMO-0003`；金额 `CNY 120000.00`（含税，税率13%）；事件ID `evt-sign-legacy-0003`；请求ID `req-void-legacy-0003`；前状态 `SIGNED/WON`，后状态保持 `SIGNED/WON`；ERP返回 `orderNo=ERP-20260718-0003（已存在）`；结论：作废动作由状态规则阻断，既有签署和订单事实不变。

**结论**:✅ 全通过

## 补充 11 条联调与异常验收用例

以下用例均以《合同主PRD》外部签署事件契约、作废联动契约和三节点同步规则为准；状态只能由动作按钮或合法事件触发。

### 用例1：正常签署赢单并生成一份订单

| 字段 | 值 |
|------|----|
| 合同编号 | `CT20260813-0001` |
| 商机编号 | `OPP20260813-0001` |
| 客户标识 | `CUS-SH-0001` |
| 金额 | `CNY 100000.00`，含税，税率13% |
| 事件ID / 请求ID | `evt-sign-0001` / `req-void-0001`（作废请求未使用，签署请求 `ext-req-0001`） |
| 前后状态 | 合同 `PENDING_SIGN→SIGNED→ARCHIVED`；商机 `CONTRACT→WON`；`OPPORTUNITY_SYNC_STATUS=SUCCESS`、`ERP_ORDER_SYNC_STATUS=SUCCESS`、`PERFORMANCE_SYNC_STATUS=SUCCESS` |
| ERP返回值 | `orderNo=ERP-20260813-0001`，金额 `CNY 100000.00` |
| 结论 | 通过；只生成一份 ERP 订单，归档后仍保留联动结果。 |

### 用例2：重复签署事件只处理一次

| 字段 | 值 |
|------|----|
| 合同编号 | `CT20260813-0002` |
| 商机编号 | `OPP20260813-0002` |
| 客户标识 | `CUS-BJ-0002` |
| 金额 | `CNY 80000.00`，含税，税率13% |
| 事件ID / 请求ID | `evt-sign-0002`（发送两次）/ `ext-req-0002` |
| 前后状态 | 首次 `PENDING_SIGN→SIGNED`；重复事件状态不变；商机只从 `CONTRACT→WON` 一次 |
| ERP返回值 | 首次返回 `orderNo=ERP-20260813-0002`；重复返回首次处理结果 |
| 结论 | 通过；同一 `eventId` 幂等，不重复赢单、业绩或订单。 |

### 用例3：验签失败保持待签署

| 字段 | 值 |
|------|----|
| 合同编号 | `CT20260813-0003` |
| 商机编号 | `OPP20260813-0003` |
| 客户标识 | `CUS-GZ-0003` |
| 金额 | `CNY 50000.00`，不含税，税率6% |
| 事件ID / 请求ID | `evt-sign-badsig-0003` / `ext-req-0003` |
| 前后状态 | 合同 `PENDING_SIGN→PENDING_SIGN`；商机保持 `CONTRACT`；无联动节点推进 |
| ERP返回值 | `未调用` |
| 结论 | 通过；验签失败进入合同异常记录，页面展示失败原因且不得改主状态。 |

### 用例4：作废与签署回调并发

| 字段 | 值 |
|------|----|
| 合同编号 | `CT20260813-0004` |
| 商机编号 | `OPP20260813-0004` |
| 客户标识 | `CUS-SZ-0004` |
| 金额 | `CNY 120000.00`，含税，税率13% |
| 事件ID / 请求ID | `evt-sign-race-0004` / `req-void-0004` |
| 前后状态 | 作废动作与签署回调并发；按已确认事实和版本校验只接受一个结果：作废成功则 `VOIDED` 且迟到签署进异常队列，签署先确认则 `SIGNED` 且作废被阻断 |
| ERP返回值 | `未调用` |
| 结论 | 通过；不静默覆盖，冲突可追踪并由主管处理。 |

### 用例5：商机联动失败，合同保持已签署可补偿

| 字段 | 值 |
|------|----|
| 合同编号 | `CT20260813-0005` |
| 商机编号 | `OPP20260813-0005` |
| 客户标识 | `CUS-HZ-0005` |
| 金额 | `CNY 200000.00`，含税，税率13% |
| 事件ID / 请求ID | `evt-sign-0005` / `req-opp-sync-0005` |
| 前后状态 | 合同 `PENDING_SIGN→SIGNED`；`OPPORTUNITY_SYNC_STATUS=PENDING→FAILED→SUCCESS`；商机 `CONTRACT→WON` |
| ERP返回值 | 联动失败期间 `未调用`；补偿成功后 `orderNo=ERP-20260813-0005` |
| 结论 | 通过；合同签署事实不回滚，主管可重试并继续后置节点。 |

### 用例6：ERP下推超时先查幂等结果

| 字段 | 值 |
|------|----|
| 合同编号 | `CT20260813-0006` |
| 商机编号 | `OPP20260813-0006` |
| 客户标识 | `CUS-NJ-0006` |
| 金额 | `CNY 68000.00`，不含税，税率9% |
| 事件ID / 请求ID | `evt-sign-0006` / `req-erp-0006` |
| 前后状态 | 合同 `SIGNED`；商机 `WON`；`ERP_ORDER_SYNC_STATUS=PROCESSING（查单中）→SUCCESS` |
| ERP返回值 | 首次超时；幂等查询返回 `orderNo=ERP-20260813-0006`、金额 `CNY 74120.00` |
| 结论 | 通过；超时不重复创建，查询结果作为正式返回。 |

### 用例7：ERP重试成功且只生成一份订单

| 字段 | 值 |
|------|----|
| 合同编号 | `CT20260813-0007` |
| 商机编号 | `OPP20260813-0007` |
| 客户标识 | `CUS-CD-0007` |
| 金额 | `CNY 90000.00`，含税，税率13% |
| 事件ID / 请求ID | `evt-sign-0007` / `req-erp-0007`（重试沿用） |
| 前后状态 | 合同 `SIGNED`；商机 `WON`；`ERP_ORDER_SYNC_STATUS=FAILED→RETRYING→SUCCESS` |
| ERP返回值 | 首次 `E_STOCK_TIMEOUT`；重试返回已有 `orderNo=ERP-20260813-0007` |
| 结论 | 通过；沿用原幂等键重试，订单数量仍为1。 |

### 用例8：合同金额与 ERP 金额不一致

| 字段 | 值 |
|------|----|
| 合同编号 | `CT20260813-0008` |
| 商机编号 | `OPP20260813-0008` |
| 客户标识 | `CUS-WH-0008` |
| 金额 | 合同 `CNY 100000.00` 含税13%；商机预计 `CNY 95000.00` |
| 事件ID / 请求ID | `evt-sign-0008` / `req-erp-0008` |
| 前后状态 | 合同 `PENDING_SIGN→SIGNED`；商机 `CONTRACT→WON`；ERP `PENDING→SUCCESS` |
| ERP返回值 | `orderNo=ERP-20260813-0008`，ERP金额 `CNY 98000.00` |
| 结论 | 通过；CRM 保留三层金额、币种/含税标识/税率并要求填写差异原因，ERP金额只读展示。 |

### 用例9：创建失败时商机不进入 CONTRACT

| 字段 | 值 |
|------|----|
| 合同编号 | `CT20260813-0009`（预生成失败，不落库） |
| 商机编号 | `OPP20260813-0009` |
| 客户标识 | `CUS-TJ-0009` |
| 金额 | `CNY 30000.00`，含税，税率13% |
| 事件ID / 请求ID | `无` / `req-create-0009` |
| 前后状态 | 商机保持 `NEGOTIATION`，合同无主数据记录；无绑定 |
| ERP返回值 | `未调用` |
| 结论 | 通过；创建失败保留表单输入和审计，不产生孤立合同或错误合同阶段。 |

### 用例10：作废解绑回退部分失败进入补偿

| 字段 | 值 |
|------|----|
| 合同编号 | `CT20260813-0010` |
| 商机编号 | `OPP20260813-0010` |
| 客户标识 | `CUS-XA-0010` |
| 金额 | `CNY 45000.00`，含税，税率13% |
| 事件ID / 请求ID | `evt-void-0010` / `req-void-0010` |
| 前后状态 | 合同 `PENDING_SIGN→VOIDED`；解绑成功；商机回退失败，`作废联动状态=COMPENSATING`，补偿重试后商机 `CONTRACT→NEGOTIATION` |
| ERP返回值 | `未调用` |
| 结论 | 通过；保留作废事实，展示待补偿和重试次数，不出现商机锁定无记录。 |

### 用例11：已作废合同收到迟到签署事件

| 字段 | 值 |
|------|----|
| 合同编号 | `CT20260813-0011` |
| 商机编号 | `OPP20260813-0011` |
| 客户标识 | `CUS-QD-0011` |
| 金额 | `CNY 76000.00`，不含税，税率6% |
| 事件ID / 请求ID | `evt-late-sign-0011` / `ext-req-0011` |
| 前后状态 | 合同已为 `VOIDED`，收到迟到事件后仍为 `VOIDED`；商机保持 `NEGOTIATION`；不推进任何节点 |
| ERP返回值 | `未调用` |
| 结论 | 通过；事件进入异常队列，保留事件来源和接收时间，不能复活合同或创建订单。 |

**补充用例结论**：✅ 11 条用例均覆盖合同编号、商机编号、客户标识、金额、事件ID、请求ID、前后状态、ERP返回值和结论；覆盖正常、幂等、验签失败、并发、补偿、超时、金额差异、创建失败和迟到事件路径。

### 用例口径补充

- 签署日期只使用外部 `signedAt`；`receivedAt` 和 `mockedAt` 分别记录接收时间与 Mock 操作时间。
- 失败事件、作废补偿和 ERP 超时均保留错误编号、请求标识、原始行号（如来自导入/批量错误包）和可重试记录。

| 日期 | 版本 | 变更摘要 |
|------|------|----------|
| 2026-08-13 | V1.1 | 补齐 11 条合同签署、作废联动、ERP 幂等、金额差异和迟到事件验收用例及时间口径 |
