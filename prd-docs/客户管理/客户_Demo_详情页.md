# 客户_Demo_详情页

> **版本**：V2.1 | 2026-08-13
> **页面类型**：PC Web 360° 详情页
> **前置文档**：《客户主PRD》《客户字段清单》
> **SSOT 声明**：基础档案来自 ERP 且只读；CRM 只展示快照、关联数据和 AI 结果。
> **字段约束**：字段定义与展示口径引用《客户字段清单》，本文不重复定义。

---

## 1. 页面概述

| 项目 | 内容 |
|------|------|
| 页面名称 | 客户 360° 详情 |
| 页面路径 | `/accounts/{id}` |
| 页面目标 | 在一个页面查看客户快照、风险、商机、订单、跟进与拜访 |
| 进入方式 | 客户列表、商机关联客户、全局深链接 |
| 数据权限 | 引用《客户主PRD》数据可见范围 |
| 页面骨架 | 页头 → 流失风险 Banner → 快照摘要 → 关系概览 → 关联商机 → ERP 订单 → 跟进聚合 → 拜访 → 底部操作 |
| 页面宽度 | 最大 1280px，居中布局 |

360° 视图原则：

- ERP 基础快照与 CRM 运营聚合明确分区。
- 基础快照不提供编辑控件。
- 每个关联区块独立加载、独立失败。
- 高流失风险在首屏形成明确行动提示。
- 新建商机、跟进和拜访属于关联动作，不改变客户档案。
- 停用客户保留历史视图并隐藏新业务动作。
- 不提供删除客户能力。
- 客户页不提供“新增客户”入口；线索转客户由线索模块提交 ERP 建档申请，CRM 只在 ERP 成功事件后创建只读快照。
- `crmCustomerId` 标识 CRM 快照，`erpCustomerId` 标识 ERP 正式档案并作为跨系统请求主键；公司名称不作为关联键。

---

## 2. 初始化与容错

### 2.1 加载顺序

1. 校验客户查看权限。
2. 拉取客户快照与同步元数据。
3. 拉取最新成功流失风险。
4. 并行拉取商机、ERP 订单和跟进聚合。
5. 并行拉取拜访摘要与统计数据。
6. 根据客户可用性与权限计算操作入口。

### 2.2 页面状态

| 场景 | 页面表现 | 可执行动作 |
|------|----------|------------|
| 首次加载 | 固定尺寸骨架屏 | 等待 |
| 客户不存在 | 404 状态页 | 返回客户列表 |
| 无权限 | 403 状态页 | 返回客户列表 |
| 主快照加载失败 | 整页错误态 | 重试、返回列表 |
| 同步中且有最近成功快照 | 展示最近成功快照 + 同步中提示 | 可继续关联动作，提交前重新校验 |
| 同步中且无成功快照 | 待同步状态 | 禁止新建关联业务，管理员可重试 |
| 单个关联区块失败 | 区块错误态 | 局部重试 |
| 同步异常但有旧快照 | 显示旧快照 + 同步异常提示 | 可继续关联动作但提交前重校验；管理员可重试 |
| 同步异常且无旧快照 | 待同步/错误态 | 禁止新建关联业务 |
| ERP已停用 | 顶部停用提示 + 历史只读 | 返回、查看历史 |
| 已合并源客户 | 顶部合并提示 + 历史只读 | 跳转主客户 |

### 2.3 独立容错

- 商机加载失败不影响 ERP 快照。
- 订单加载失败不影响商机和跟进。
- 跟进聚合失败不影响风险 Banner 展示。
- AI 失败不影响客户基础信息。
- 任一区块重试只刷新该区块。
- 主快照不存在时不展示伪造客户信息。
- 任何页面动作提交前重新校验客户生命周期、ERP 可用性、`snapshotVersion` 与 `sourceEventId`。

---

## 3. 页头与同步状态

### 3.1 页头左侧

- 返回按钮使用 ArrowLeft 图标。
- 标题展示公司名称。
- 副标题展示 `erpCustomerId`，并按权限展示 `crmCustomerId`。
- 双标识均支持复制；`erpCustomerId` 用于跨系统对账与请求。
- 公司名称超长时两行省略。
- 返回时恢复客户列表筛选与页码。

### 3.2 页头右侧

| 元素 | 展示 | 交互 |
|------|------|------|
| 客户等级 | Tag，口径引用字段清单 | 无 |
| 客户生命周期 | `PROSPECT/ACTIVE/DISABLED/MERGED` Tag | 只读；由 ERP 事件变更 |
| ERP同步标识 | 技术元数据 | Tooltip 查看同步时间 |
| 快照版本 | `snapshotVersion` + 更新时间 | 只读 |
| 刷新 | RefreshCw 图标 | 局部刷新全部区块 |

### 3.3 同步异常提示

- 提示文案：`ERP 快照同步异常，当前展示最近一次成功数据。`。
- 展示最后成功同步时间。
- 销售只可查看。
- 管理员显示“重试同步”。
- 重试前弹二次确认。
- 重试不清空当前快照。
- 同步成功后提示条消失。

同步处理状态只使用 `PENDING_RECEIVE/VALIDATING/AVAILABLE/SYNC_FAILED`，不把 `DISABLED` 当作同步状态。同步失败沿用最近成功快照；没有成功快照时不展示伪造客户。

### 3.4 ERP 停用提示

- 使用灰色全宽提示条。
- 文案：`该客户已在 ERP 停用，历史数据仅供查看。`。
- 隐藏新建商机、添加跟进和创建拜访。
- 不隐藏历史商机、订单或跟进。
- 不提供 CRM 恢复按钮。
- `DISABLED` 不是终态；恢复必须收到 ERP 带更高 `sourceVersion` 和新 `sourceEventId` 的合法启用事件，依次经过待接收、校验中后才恢复 `ACTIVE/AVAILABLE`。
- 启用事件缺版本、事件重复、版本过旧或校验失败时保持 `DISABLED`。
- 恢复后历史商机、合同、拜访、跟进和订单不自动改状态或回滚，只重新开放符合权限的新业务动作。

---

## 4. AI 流失风险 Banner

### 4.1 Banner 位置

- 位于页头下方、快照卡片上方。
- 宽度与主内容一致。
- 高度自适应，最小 72px。
- 不使用浮动卡片覆盖内容。
- 风险文案与行动按钮同一水平区。

### 4.2 风险状态

| 风险场景 | 视觉 | 主文案 | 操作 |
|----------|------|--------|------|
| 高风险 | 红色左边框 + AlertTriangle | `该客户存在较高流失风险，建议尽快跟进。` | 添加跟进 |
| 中风险 | 黄色左边框 | `该客户需要持续关注。` | 查看风险依据 |
| 低风险 | 绿色左边框 | `当前客户关系活跃。` | 查看风险依据 |
| 待计算 | 灰色 | `流失风险正在计算。` | 刷新 |
| 失败且有历史结果 | 历史风险色 + Clock | `风险计算失败，当前展示上次结果。` | 主管重试 |
| 失败且无历史结果 | 灰色 + AlertCircle | `流失风险暂不可用。` | 主管重试 |

颜色规范：高风险红色、中风险黄色、低风险绿色；待计算、失败无历史结果使用灰色；`DISABLED` 使用灰色停用标识，停用颜色不代表 AI 风险等级。

### 4.3 风险依据

- 展示命中的业务条件摘要。
- 展示最近跟进时间。
- 展示进行中商机数量。
- 展示最长阶段停留摘要。
- 展示最近成功计算时间。
- 不展示模型内部权重和技术参数。
- 风险依据使用 Popover 展开。

### 4.4 高风险后置结果

| 动作 | 展示 |
|------|------|
| CRM 跟进提醒 | 已创建、处理中、失败 |
| ERP 信用冻结通知 Mock | 已发送、处理中、失败 |

展示约束：

- 风险高不等于 ERP 已冻结信用。
- ERP 通知失败时显示管理员重试入口。
- 重复重试使用同一幂等键。
- 后置动作失败不改变风险 Tag。
- 停用客户不生成新的自动跟进提醒。

---

## 5. ERP 客户快照卡片

### 5.1 卡片结构

- 标题：`客户快照`。
- 右上角说明：`数据来自 ERP`。
- 使用 Description KV 三列布局。
- 所有值只读。
- 标签宽度 96px。
- 空值显示 `--`。
- 不展示编辑图标。

### 5.2 KV 清单

> 本表只描述详情卡片布局与交互；字段语义、枚举、状态和同步元数据均引用《客户字段清单》，不在 Demo 重复定义。

| 字段名 | 展示形态 | 交互 |
|--------|----------|------|
| 客户编号 | 等宽文本 | 复制 |
| 公司名称 | 普通文本 | 无 |
| 联系人 | 普通文本 | 无 |
| 手机号 | 按权限显示 | 有权限时复制 |
| 所属行业 | 普通文本 | 无 |
| 所在地区 | 普通文本 | 无 |
| 客户等级 | Tag | 无 |
| 信用额度 | 格式化金额 | 无 |

### 5.3 SSOT 提示

- 卡片底部固定显示 `客户正式档案由 ERP 维护`。
- 不提供跳转外部 ERP 的编辑入口。
- 数据有误时引导联系管理员核对 ERP。
- 同步异常时卡片顶部显示旧快照标识。
- 不在 CRM 内临时修改作为补救。

---

## 6. CRM 关系概览

### 6.1 指标布局

使用一行六列紧凑指标，不使用独立嵌套卡片：

| 指标 | 数据来源 | 点击行为 |
|------|----------|----------|
| 关联商机数 | CRM 聚合 | 滚动到商机区块 |
| 最近商机阶段 | CRM 聚合 | 滚动到商机区块 |
| 关联订单数 | ERP 聚合 | 滚动到订单区块 |
| 累计成交金额 | ERP 已确认订单金额聚合 | 滚动到订单区块 |
| 最近跟进时间 | CRM 聚合 | 滚动到跟进区块 |
| AI流失风险 | AI 最新成功结果 | 滚动到风险 Banner |

### 6.2 指标稳定性

- 每格固定最小高度 72px。
- 指标名称单行。
- 数值超长使用 Tooltip。
- 加载时保持固定宽度骨架。
- 单指标失败显示 `--`，不改变布局。
- 计数与下方区块总数必须一致。

### 6.3 跨模块关联与只读边界

| 关联对象 | 内部关联 | 跨系统关联 | 权威字段清单/来源 | 客户详情页能力 |
|----------|----------|------------|-------------------|----------------|
| 商机 | `crmCustomerId` | 请求携带 `erpCustomerId` | 《商机字段清单》；客户身份引用《客户字段清单》 | 可查看/创建，基础客户字段只读 |
| 合同 | `crmCustomerId` | 请求携带 `erpCustomerId` | 《合同字段清单》；客户身份引用《客户字段清单》 | 可查看，合同须从商机详情发起，停用时只读 |
| 拜访 | `crmCustomerId` | 请求携带 `erpCustomerId` | 《拜访管理/拜访计划字段清单》；客户身份引用《客户字段清单》 | 可查看/创建；停用阻断新建/签到，已签到可完成并生成跟进 |
| 跟进 | `crmCustomerId` + 来源对象标识 | 必要时携带 `erpCustomerId` | 跟进记录对象字段 SSOT；客户身份引用《客户字段清单》 | 可查看/新增，停用时禁止新增 |
| ERP 订单 | 不复制 CRM 主键 | `erpCustomerId` | ERP 订单接口契约/订单字段 SSOT | 只读展示，不可编辑 |

详情页不重复定义跨模块字段；公司名称只展示，不作为上述任何对象的关联键。

---

## 7. 关联商机表格

### 7.1 区块头

| 元素 | 规格 |
|------|------|
| 标题 | `关联商机 (N)` |
| 筛选 | 进行中、赢单、输单 |
| 主按钮 | `新建商机`，可用且有权限时展示 |
| 展开 | 默认展示最近 5 条，支持查看全部 |

新建商机行为：

- 跳转 `/opportunities/new?customerId={id}`。
- 自动预填当前客户。
- 不允许在商机页新建客户。
- ERP已停用时不展示按钮。
- 同步中/同步异常且有最近成功快照时允许进入，但提交前再次校验客户可用性、快照版本和事件标识。
- 没有最近成功快照时不展示新建商机入口。

### 7.2 表格列

| 列名 | 宽度 | 固定 | 对齐 | 展示规则 |
|------|------|------|------|----------|
| 商机编号 | 168px | 左 | 左 | 链接进入商机详情 |
| 商机名称 | 220px | 否 | 左 | 两行省略 |
| 商机阶段 | 132px | 否 | 中 | Tag 引用商机字段清单 |
| 预计金额 | 140px | 否 | 右 | 格式化金额 |
| AI 成交概率 | 128px | 否 | 中 | 进度 + 数字 |
| 预计成交日期 | 144px | 否 | 中 | 无值 `--` |
| 阶段变更时间 | 176px | 否 | 中 | 完整时间 |
| 操作 | 96px | 右 | 中 | 查看 |

### 7.3 表格行为

- 默认按阶段变更时间倒序。
- 终态与进行中均保留。
- 表头吸顶仅在区块内部滚动时生效。
- 最多 5 条时不展示分页。
- 查看全部后使用区块内分页。
- 查询失败显示 `商机数据加载失败` 与重试。
- 空态显示 `该客户暂无关联商机`。
- 有权限且客户可用时空态提供“新建商机”。

---

## 8. ERP 关联订单表格

### 8.1 区块头

| 元素 | 规格 |
|------|------|
| 标题 | `关联订单 (N)` |
| 来源标识 | `数据来自 ERP` |
| 查看全部 | 展开区块内分页 |
| 刷新 | 只刷新订单摘要 |

### 8.2 表格列

| 列名 | 宽度 | 固定 | 对齐 | 展示规则 |
|------|------|------|------|----------|
| 订单编号 | 168px | 左 | 左 | 链接样式，打开 ERP Mock 详情 |
| 订单日期 | 144px | 否 | 中 | 日期 |
| 订单金额 | 148px | 否 | 右 | 格式化金额 |
| 订单状态 | 120px | 否 | 中 | 使用 ERP 返回文案，不在本文定义 |
| 来源商机 | 168px | 否 | 左 | 有关联时进入商机详情 |
| 最近同步时间 | 176px | 否 | 中 | 完整时间 |
| 操作 | 96px | 右 | 中 | 查看 |

### 8.3 订单交互

- 订单数据只读。
- CRM 不提供编辑、取消或删除订单。
- 点击订单编号在新窗口打开 ERP Mock 详情。
- 外链打开前展示来源提示。
- 订单同步失败仅该区块错误。
- 空态显示 `ERP 暂无关联订单`。
- 空态不推断客户从未成交。
- WMS 履约不在此区块提供入口。

---

## 9. 跟进聚合组件

### 9.1 聚合范围

时间线聚合以下来源：

- 客户直接跟进。
- 转化前线索跟进。
- 客户名下商机跟进。
- 拜访完成自动生成的跟进记录。

聚合约束：

- 每条记录保留原来源类型。
- 每条记录保留原来源标识。
- 不复制成新的无来源记录。
- 按跟进时间统一倒序。
- 无权来源对象不进入结果。

### 9.2 区块头

| 元素 | 规格 |
|------|------|
| 标题 | `跟进记录` |
| 计数 | 聚合总条数 |
| 主按钮 | `添加跟进`，客户可用且有权限时展示 |
| 导出 | 主管或管理员可用 |

### 9.3 筛选控件

| 条件 | 控件 | Placeholder | 行为 |
|------|------|-------------|------|
| 来源类型 | MultiSelect | `全部来源` | 按来源过滤 |
| 跟进方式 | Select | `全部方式` | 选项引用跟进记录 SSOT |
| 跟进人 | SelectWithSearch | `全部跟进人` | 按数据权限加载 |
| 跟进时间 | DateRangePicker | `开始日期 - 结束日期` | 范围过滤 |

### 9.4 时间线项

每条记录展示：

- 跟进方式图标。
- 跟进人。
- 跟进时间。
- 跟进内容。
- 下次计划（存在时）。
- 来源 Tag。
- 来源编号链接。
- “来自拜访”自动生成标识。

时间线交互：

- 首屏 10 条。
- 点击“加载更多”追加 10 条。
- 新增成功插入顶部。
- 内容超过 6 行支持展开。
- 来源链接按权限跳转。
- 来源已失效时保留只读文本。

### 9.5 添加客户跟进

- 点击打开右侧抽屉。
- 关联对象固定为当前客户。
- 字段规格引用跟进记录 SSOT。
- 保存成功关闭抽屉并刷新风险提示的“数据已变化”状态。
- 新跟进不立即伪造低风险结果。
- 下一次定时计算或人工重算后更新风险。
- 保存失败保留用户输入。
- ERP已停用时不展示入口。
- 同步中/同步异常且有最近成功快照时可新增，但提交前必须重新校验。
- 没有最近成功快照或客户为 `DISABLED/MERGED` 时不展示入口；停用客户历史跟进只读可查。

---

## 10. 关联拜访区块

### 10.1 展示

| 列名 | 宽度 | 展示 |
|------|------|------|
| 拜访编号 | 168px | 链接进入拜访详情 |
| 拜访标题 | 220px | 两行省略 |
| 拜访方式 | 112px | 文本 |
| 计划拜访时间 | 176px | 完整时间 |
| 拜访状态 | 120px | Tag 引用拜访字段清单 |
| 操作 | 96px | 查看 |

### 10.2 交互

- 默认展示最近 3 条。
- 标题展示总条数。
- “创建拜访”预填当前客户。
- 停用客户不展示创建入口。
- 完成拜访后跟进聚合出现对应记录。
- 空态显示 `暂无关联拜访`。

---

## 11. 底部操作栏

### 11.1 正常可用客户

| 位置 | 操作 | 权限 |
|------|------|------|
| 左侧 | 返回列表 | 全部 |
| 右侧 | 添加跟进 | 有跟进权限 |
| 右侧 | 创建拜访 | 有拜访权限 |
| 右侧主按钮 | 新建商机 | 有商机权限 |

### 11.2 特殊状态矩阵

| 场景 | 返回 | 添加跟进 | 创建拜访 | 新建商机 / 跳转商机发起合同 | 重试同步 |
|------|:----:|:--------:|:--------:|:-------------:|:--------:|
| `ACTIVE + AVAILABLE` | ✅ | 按权限 | 按权限 | 按权限 | ❌ |
| `ACTIVE + PENDING_RECEIVE/VALIDATING` 且有最近成功快照 | ✅ | 按权限，提交前重校验 | 按权限，提交前重校验 | 按权限，提交前重校验 | 管理员 |
| `ACTIVE + SYNC_FAILED` 且有最近成功快照 | ✅ | 按权限，提交前重校验 | 按权限，提交前重校验 | 按权限，提交前重校验 | 管理员 |
| 无最近成功快照 | ✅ | ❌ | ❌ | ❌ | 管理员 |
| `DISABLED` ERP已停用 | ✅ | ❌，历史只读 | ❌ | ❌ | 按同步任务处理，不是恢复 |
| `MERGED` 源客户 | ✅ | ❌ | ❌ | ❌，跳转主客户 | ❌ |

操作约束：

- 不展示编辑客户。
- 不展示删除客户。
- 不展示停用客户。
- 不可用动作不渲染。
- 底部栏高度固定 64px。
- 按钮提交中防重复点击。

### 11.3 停用客户关联对象处理矩阵

| 关联对象 | 停用期间 | 恢复后 |
|----------|----------|--------|
| 商机 | 历史可查看，不允许推进或新建 | 不自动改变阶段，按权限继续未完商机 |
| 合同 | 只读，不自动作废 | 不自动恢复或改状态 |
| 拜访 | 新建/改期新单/新签到阻断；已签到可完成并生成跟进，属于历史执行事实收口 | 不自动恢复执行，需重新安排或新建 |
| 跟进 | 历史可查，统一禁止新增 | 恢复且快照可用后按权限新增 |
| 订单 | 继续 ERP 只读同步 | 继续按 `erpCustomerId` 同步 |
| 新业务 | 禁止新建商机、拜访、跟进；合同必须从商机详情发起 | 恢复后按权限开放 |

### 11.4 ERP 合并事件

- CRM 不执行合并，只接收带 `sourceErpCustomerId`、`targetErpCustomerId`、`sourceVersion`、`sourceEventId` 的 `mergeEvent`。
- 源客户改为 `MERGED` 只读，页头提示 `该客户已合并至 XXX` 并提供主客户跳转。
- 历史商机、合同、拜访和跟进保留原关联，同时支持跳转主客户；新业务只能关联 target。

---

## 12. 二次确认与反馈

### 12.1 重试同步

| 项目 | 文案 |
|------|------|
| 标题 | `重试同步` |
| 内容 | `将重新拉取 ERP 最新客户快照，当前数据不会被清空。` |
| 取消 | `取消` |
| 确认 | `确认重试` |

### 12.2 ERP 外链

| 项目 | 文案 |
|------|------|
| 标题 | `前往 ERP` |
| 内容 | `将在新窗口打开 ERP 订单详情。` |
| 取消 | `取消` |
| 确认 | `继续查看` |

### 12.3 Toast

| 场景 | 文案 |
|------|------|
| 同步任务提交 | `同步任务已重新提交` |
| 同步重试失败 | `同步重试失败，请稍后再试` |
| 风险重算成功 | `流失风险已更新` |
| 风险重算失败 | `风险计算失败，已保留上次结果` |
| 跟进保存成功 | `跟进记录已添加` |
| 客户停用阻断 | `当前客户不可用于新业务，请联系管理员核对 ERP 状态` |
| 无权限 | `无权执行该操作` |

---

## 13. Mock 详情数据

### 13.1 主场景：强盛科技

- 客户等级为字段清单中的高等级展示场景。
- ERP 同步标识为已同步。
- AI 流失风险为中风险。
- 关联商机 3 条。
- 商机分布：1 条赢单、1 条商务谈判、1 条初步接触。
- ERP 订单 2 条。
- 跟进记录 8 条。
- 其中 2 条来自线索。
- 其中 3 条来自商机。
- 其中 1 条来自客户直接跟进。
- 其中 2 条来自拜访完成。
- 关联拜访 3 条。

### 13.2 补充场景

- 高风险客户：Banner 显示 CRM 提醒和 ERP 通知结果。
- AI 失败：保留历史风险并显示重试。
- 同步异常：展示旧快照和管理员重试。
- ERP停用：所有新业务动作隐藏。
- 同步中且有旧快照：允许关联动作，提交前重新校验并展示旧版本提示。
- 同步中且无旧快照：禁止新关联业务，展示待同步状态。
- ERP恢复：模拟带更高版本和新事件ID的启用事件；CRM无恢复按钮。
- ERP合并：源客户展示合并提示、历史关联和主客户跳转。
- 停用关联对象：商机不可推进、合同只读不自动作废、拜访新建/改期新单/新签到阻断；已签到拜访允许完成并生成跟进作为历史事实收口；跟进禁止新增、订单只读同步。
- 无订单：订单区块显示空态。
- 商机区块失败：其他区块正常。
- 超长公司名称：页头与表格正确省略。

---

## 14. 键盘与可访问性

- 所有图标按钮有 aria-label 和 Tooltip。
- Tab 顺序与视觉顺序一致。
- Banner 风险变化通过 aria-live 播报。
- 表格链接可键盘聚焦。
- Popover 支持 Esc 关闭。
- 区块加载状态有可读文本。
- 颜色不是风险状态的唯一表达方式。
- 固定底栏不遮挡页面最后内容。

---

## 15. 验收清单

- [ ] 客户基础快照全部只读且标明 ERP 来源。
- [ ] 高、中、低、待计算和失败 Banner 均有明确状态。
- [ ] 高风险后置结果区分 CRM 提醒与 ERP 通知。
- [ ] 关联商机表格包含固定列、宽度和详情跳转。
- [ ] 关联订单只读且不提供 CRM 修改动作。
- [ ] 跟进聚合保留来源类型和来源标识。
- [ ] 拜访完成记录进入跟进聚合。
- [ ] 各区块独立加载和失败。
- [ ] 停用客户保留历史但隐藏新业务动作。
- [ ] 双标识、快照版本和同步元数据可展示且只读。
- [ ] 生命周期状态与同步处理状态分离；`DISABLED` 标识为可恢复的不可用状态。
- [ ] 同步中/异常动作与最近成功快照规则一致；无旧快照时禁止新业务。
- [ ] ERP恢复和合并事件按主PRD规则处理。
- [ ] 停用客户关联对象处理与列表页、主PRD一致，尤其是禁止新增跟进。
- [ ] 页面没有编辑、删除或停用客户入口。

---

## 16. 修订记录

| 日期 | 版本 | 变更摘要 |
|------|------|----------|
| 2026-07-17 | V1.0 | 初版 |
| 2026-07-18 | V2.0 | 补齐 360° 结构、流失 Banner、ERP 快照、商机与订单表格、跟进聚合、拜访和底部动作 |
| 2026-08-13 | V2.1 | 按客户模块审计补齐双标识、状态分离、同步中动作矩阵、ERP恢复/合并与停用关联对象规则 |
