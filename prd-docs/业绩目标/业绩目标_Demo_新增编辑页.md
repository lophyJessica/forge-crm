# 业绩目标_Demo_新增编辑页

> **版本**：V2.1 | 2026-08-13
> **页面类型**：PC Web 新增页 / 编辑页
> **前置文档**：《业绩目标主PRD》《业绩目标字段清单》
> **字段约束**：字段定义、必填性、格式和限制以《业绩目标字段清单》为唯一权威源。

---

## 1. 页面概述

| 项目 | 新增模式 | 编辑模式 |
|------|----------|----------|
| 页面名称 | 新建业绩目标 | 编辑业绩目标 |
| 页面路径 | `/performance-targets/new` | `/performance-targets/{id}/edit` |
| 进入方式 | 列表“新建目标” | 列表“编辑” |
| 页面目标 | 为销售创建月度三项目标 | 调整尚未结算的三项目标值 |
| 可用状态 | 新建 | 仅 `ACTIVE + NOT_STARTED` |
| 页面骨架 | 页头 + 目标表单 + 编号预览 + 固定底栏 | 页头 + 状态摘要 + 目标表单 + 当前进度只读 + 固定底栏 |

页面原则：

- 使用销售ID而不是姓名建立目标归属。
- 销售与月份创建后不可更换。
- 三项目标值由主管维护。
- 三项当前进度由系统聚合。
- 目标主状态与结算处理状态由系统结算。
- 表单不提供状态编辑。
- `PROCESSING/RETRYING` 结算锁生效时禁止编辑；`FAILED` 必须通过列表“重试结算”动作处理。
- 已结算目标不可进入编辑页。
- 不提供删除。
- 表单最大宽度 920px。

---

## 2. 路由与初始化

### 2.1 新增模式

- 校验目标新增权限。
- 从列表进入时预填列表当前月份。
- 销售默认为空。
- 三项目标值为空。
- 当前进度不展示或显示初始只读摘要。
- 状态不展示。
- 目标编号显示预览占位。
- 不在前端最终生成编号。

### 2.2 编辑模式

- 根据路径标识加载目标。
- 校验查看权限。
- 校验编辑权限。
- 校验目标组合为 `ACTIVE + NOT_STARTED` 且锁状态为 `UNLOCKED`。
- 展示目标编号、目标主状态和结算处理状态只读摘要。
- 销售和月份只读。
- 三项目标值可编辑。
- 三项当前进度只读展示。
- `PROCESSING/RETRYING` 或 `SUCCESS` 目标直接跳转列表/详情，不进入编辑。
- `FAILED` 目标不进入编辑，引导回列表点击“重试结算”。
- Toast `当前目标不可编辑，请按结算处理状态操作`。

### 2.3 路由参数

| 参数 | 来源 | 用途 | 异常处理 |
|------|------|------|----------|
| `id` | 编辑路径 | 查询目标 | 不存在进入 404 |
| `month` | 列表月份 | 预填目标月份 | 非法值回当前月 |
| `from` | 来源页 | 返回上下文 | 非法值回目标列表 |

### 2.4 进入页面校验

- 销售本人无新增或编辑权限时返回 403。
- 主管只能为团队成员创建目标。
- 管理员可选择全部已映射销售。
- 未映射销售ID的用户不进入候选。
- 编辑页面若结算锁已生效，立即跳转只读列表或详情上下文。
- 结算锁生效时间以字段清单的 `锁定时间` 为准；前端状态只是展示，最终以保存动作的后端校验为准。

---

## 3. 页面布局

### 3.1 页头

| 元素 | 新增模式 | 编辑模式 |
|------|----------|----------|
| 返回 | 返回列表 | 返回列表 |
| 标题 | 新建业绩目标 | 编辑业绩目标 |
| 副标题 | `设置销售月度目标` | 目标编号 |
| 状态 Tag | 不展示 | 进行中，只读 |

### 3.2 表单区

- 使用一个“目标设置”卡片。
- 卡片圆角 6px。
- 使用两列栅格。
- 销售与目标月份各占一列。
- 三项目标使用三列等宽布局。
- 编辑模式下当前进度使用紧凑只读区块。
- 不在卡片中嵌套卡片。
- 错误文案在控件下方。

### 3.3 编号预览

- 新增模式选择销售与月份后展示目标编号预览。
- 预览依据字段清单规则和销售ID映射。
- 预览标注 `最终编号以保存结果为准`。
- 销售或月份为空时显示 `选择销售和月份后生成`。
- 编辑模式展示正式目标编号，不再重算。

---

## 4. 字段控件规格

> “限制与校验”只引用字段清单，不在本文重复具体数值、枚举或必填性。

### 4.1 销售

| 项目 | 规格 |
|------|------|
| 字段名 | 销售 |
| 控件类型 | SelectWithSearch |
| Placeholder | `请输入销售姓名或销售ID搜索` |
| 宽度 | 100% |
| 必填性 | 引用《业绩目标字段清单》 |
| 数据来源 | 销售主数据 Mock + 销售ID映射 |
| 联动 | 选择后刷新编号预览和唯一性预检 |
| 编辑模式 | 只读，不可更换 |

候选展示：

- 第一行展示销售姓名。
- 第二行展示销售ID和团队。
- 搜索支持姓名和销售ID。
- 同名销售通过ID区分。
- 未映射销售不展示。
- 停用销售是否可选由销售主数据规则决定。
- 主管只看到团队成员。

选择行为：

- 保存真实关联使用销售ID。
- 姓名只作展示快照。
- 选择后调用销售+月份唯一性预检。
- 已存在目标时展示现有目标链接。
- 不允许创建重复目标。

### 4.2 目标月份

| 项目 | 规格 |
|------|------|
| 字段名 | 目标月份 |
| 控件类型 | MonthPicker |
| Placeholder | `请选择目标月份` |
| 宽度 | 100% |
| 格式与必填性 | 引用《业绩目标字段清单》 |
| 时区 | Asia/Shanghai |
| 联动 | 变化后刷新编号预览和唯一性预检 |
| 编辑模式 | 只读，不可更换 |

月份交互：

- 默认使用路由预填月份。
- 支持当前月和未来月。
- 历史月份新建是否允许由业务权限规则控制；Demo 默认阻断。
- 选择当前月时显示月内设置提示。
- 浏览器时区不改变月份值。
- 编辑后不允许更换月份，避免编号与归属漂移。

### 4.3 线索目标数

| 项目 | 规格 |
|------|------|
| 字段名 | 线索目标数 |
| 控件类型 | NumberInput |
| Placeholder | `请输入线索转化目标数` |
| 宽度 | 100% |
| 数值与必填性 | 引用《业绩目标字段清单》 |
| 步进 | 整数步进器 |
| 联动 | 变化时预览线索达成率，不修改当前值 |

输入行为：

- 数值范围、非负整数精度及禁止小数/科学计数法均引用《业绩目标字段清单》。
- 粘贴内容规范化后校验。
- 不合规时控件标红。
- 当前进度大于新目标时允许保存并显示超额预览。

### 4.4 商机目标数

| 项目 | 规格 |
|------|------|
| 字段名 | 商机目标数 |
| 控件类型 | NumberInput |
| Placeholder | `请输入商机创建目标数` |
| 宽度 | 100% |
| 数值与必填性 | 引用《业绩目标字段清单》 |
| 步进 | 整数步进器 |
| 联动 | 变化时预览商机达成率，不修改当前值 |

输入行为：

- 数值范围、非负整数精度及禁止小数/科学计数法均引用《业绩目标字段清单》。
- 当前进度只读。
- 目标变化立即刷新预览。
- 预览不提前改变目标状态。

### 4.5 赢单目标金额

| 项目 | 规格 |
|------|------|
| 字段名 | 赢单目标金额 |
| 控件类型 | NumberInput |
| Placeholder | `请输入赢单目标金额` |
| 宽度 | 100% |
| 前缀 | `¥` |
| 数值与精度 | 引用《业绩目标字段清单》 |
| 联动 | 变化时预览赢单达成率，不修改当前金额 |

金额交互：

- 数值范围、金额精度及禁止负数/科学计数法均引用《业绩目标字段清单》。
- 聚焦时显示原始值。
- 失焦后格式化。
- 超出范围时标红。
- 目标变化只影响预览，不回写历史进度。

### 4.6 目标编号

| 项目 | 新增模式 | 编辑模式 |
|------|----------|----------|
| 字段名 | 目标编号 | 目标编号 |
| 展示 | 只读预览 | 正式只读文本 |
| 来源 | 销售ID+月份预览，保存由后台生成 | 后台返回 |
| 可编辑 | 否 | 否 |
| 联动 | 销售或月份变化时预览更新 | 不再变化 |

### 4.7 当前线索转化数

| 项目 | 新增模式 | 编辑模式 |
|------|----------|----------|
| 字段名 | 当前线索转化数 | 当前线索转化数 |
| 展示 | 可省略或初始只读摘要 | 只读指标 |
| 来源 | 系统聚合 | 系统聚合 |
| 可编辑 | 否 | 否 |
| 联动 | 与目标值计算预览达成率 | 目标变化时预览更新 |

### 4.8 当前商机数

| 项目 | 新增模式 | 编辑模式 |
|------|----------|----------|
| 字段名 | 当前商机数 | 当前商机数 |
| 展示 | 可省略或初始只读摘要 | 只读指标 |
| 来源 | 系统聚合 | 系统聚合 |
| 可编辑 | 否 | 否 |
| 联动 | 与目标值计算预览达成率 | 目标变化时预览更新 |

### 4.9 当前 CRM WON 金额

| 项目 | 新增模式 | 编辑模式 |
|------|----------|----------|
| 字段名 | 当前 CRM WON 金额 | 当前 CRM WON 金额 |
| 展示 | 可省略或初始只读摘要 | 只读金额指标 |
| 来源 | CRM 商机 WON 事件聚合 | CRM 商机 WON 事件聚合 |
| 可编辑 | 否 | 否 |
| 联动 | 仅作 CRM 经营指标预览 | 目标变化时展示差异，不作为结算判定 |

### 4.10 当前 ERP 确认订单金额

| 项目 | 新增模式 | 编辑模式 |
|------|----------|----------|
| 字段名 | 当前 ERP 确认订单金额 | 当前 ERP 确认订单金额 |
| 展示 | 可省略或初始只读摘要 | 只读金额指标 |
| 来源 | ERP 已确认订单事件聚合 | ERP 已确认订单事件聚合 |
| 可编辑 | 否 | 否 |
| 联动 | 与赢单目标金额计算结算口径预览 | 目标变化时预览更新 |

### 4.11 金额差异

| 项目 | 新增模式 | 编辑模式 |
|------|----------|----------|
| 字段名 | 金额差异 | 金额差异 |
| 展示 | 可省略 | `当前 CRM WON 金额 - 当前 ERP 确认订单金额` |
| 来源 | CRM/ERP 实时聚合结果 | CRM/ERP 实时聚合结果 |
| 可编辑 | 否 | 否 |
| 联动 | 不参与目标状态预览 | 用于经营分析和差异审计 |

### 4.12 目标主状态

| 项目 | 新增模式 | 编辑模式 |
|------|----------|----------|
| 字段名 | 目标状态 | 目标状态 |
| 展示 | 不展示 | 只读 Tag |
| 来源 | 创建与月末结算 | 后台返回 |
| 可编辑 | 否 | 否 |
| 变化方式 | 创建后进行中；月末系统结算 | 不通过表单 |

### 4.13 结算处理状态与锁状态

| 字段 | 新增模式 | 编辑模式 |
|------|----------|----------|
| 结算处理状态 | `NOT_STARTED`，只读摘要或不展示 | `NOT_STARTED` 时允许编辑；`PROCESSING/RETRYING` 显示“结算中”；`FAILED` 引导回列表重试；`SUCCESS` 只读 |
| 锁状态 | `UNLOCKED` | `UNLOCKED` 才允许保存；`LOCKED` 阻断保存 |
| 锁定时间 | 不展示 | 有值时只读展示，引用字段清单 |
| 结算截止时间 | 不展示 | 只读展示，引用字段清单 |
| 最近结算时间 | 不展示 | 只读展示，引用字段清单 |

结算处理状态和锁状态均不可由表单修改；状态变化必须由“保存修改”“发起结算”“重试结算”等动作按钮或系统结算动作触发。

---

## 5. 当前进度只读区

### 5.1 编辑模式展示

| 指标 | 展示 | 说明 |
|------|------|------|
| 线索进度 | 当前值 / 当前目标 + 进度条 | 当前目标变化时仅预览新比例 |
| 商机进度 | 当前值 / 当前目标 + 进度条 | 当前值不可编辑 |
| CRM WON 经营进度 | 当前 CRM WON 金额 + 进度摘要 | 不作为结算判定 |
| ERP 赢单进度 | 当前 ERP 确认订单金额 / 当前目标 + 进度条 | ERP金额不可编辑，作为结算判定 |
| 金额差异 | CRM WON - ERP 确认订单金额 | 只读 |

### 5.2 预览规则

- 进度条颜色引用字段清单。
- 目标值变化后即时预览。
- 当前值不随表单输入变化。
- 超过 100% 显示真实百分比。
- 目标值为 0 时按字段清单显示 100%，不做除零。
- 预览不改变后台状态。
- 提前三项达标仍显示进行中提示。
- 保存失败时恢复后台目标比例。

### 5.3 数据时间

- 展示当前进度聚合时间。
- 聚合延迟时显示提示。
- 编辑页面不提供修正当前值。
- 如进度有误，引导查看事件明细。
- 不允许用调整目标值伪造源事件修正。

---

## 6. 字段联动规则

| 编号 | 触发 | 目标 | 行为 |
|------|------|------|------|
| L01 | 选择销售 | 目标编号预览 | 使用销售ID更新预览 |
| L02 | 选择目标月份 | 目标编号预览 | 使用月份更新预览 |
| L03 | 销售或月份变化 | 唯一性校验 | 远程预检是否已有目标 |
| L04 | 三项目标变化 | 三项达成率 | 仅前端预览；ERP金额为赢单结算预览，CRM WON仅为经营指标 |
| L05 | 编辑目标保存 | 审计日志 | 记录修改前后值 |
| L06 | 当前月目标编辑 | 二次确认 | 保存前确认目标调整影响 |
| L07 | 保存成功 | 列表进度 | 使用新目标重新展示实时比例 |
| L08 | 保存失败 | 当前目标 | 后台值不变化，表单输入保留 |

联动约束：

- 姓名变化不更新目标编号。
- 销售ID与月份创建后不可修改。
- 当前进度不可由表单联动覆盖。
- 状态不可由预览结果改变。
- 提前三项达标不提前结算。
- 结算处理状态和锁状态不可由表单联动覆盖。

---

## 7. 唯一性预检

### 7.1 触发

- 销售与月份都已选择。
- 300ms 防抖后请求后台。
- 输入未完成时不请求。
- 保存前仍执行最终唯一约束。

### 7.2 结果

| 结果 | 页面表现 |
|------|----------|
| 可创建 | 绿色提示 `该销售本月尚未设置目标` |
| 已存在 | 红色提示 + 现有目标编号链接 |
| 销售ID未映射 | 错误提示并阻断 |
| 查询失败 | 弱提示，保存前再次校验 |

### 7.3 已存在处理

- 保存按钮保持可点击但提交会阻断，或前端根据设计禁用并展示原因。
- 提供“查看现有目标”。
- 不提供覆盖创建。
- 不提供删除现有目标。
- 不通过更改姓名绕过唯一性。

---

## 8. 新增与编辑模式差异

| 能力 | 新增 | 进行中编辑 | 已结算直接访问 |
|------|:----:|:----------:|:--------------:|
| 销售 | 可选择 | 只读 | 不允许进入 |
| 目标月份 | 可选择/预填 | 只读 | 不允许进入 |
| 三项目标值 | 可编辑 | 可编辑 | 不允许进入 |
| 三项当前值 | 初始摘要 | 只读实时值 | 详情快照只读 |
| 目标编号 | 预览 | 只读正式值 | 详情只读 |
| 目标主状态 | 不展示 | 只读 | 详情只读 |
| 结算处理状态 | 不展示 | 只读；`PROCESSING/RETRYING/FAILED` 不可编辑 | 详情只读 |
| 锁状态/锁定时间 | 不展示 | 只读 | 详情只读 |
| 保存 | 创建目标 | 保存修改 | 不展示 |

编辑规则：

- 当前月编辑进入前弹一次确认。
- 未来月编辑不弹月内确认。
- 目标值可上调或下调；当前值超过新目标允许保存并显示真实超 100%。
- 每个目标每自然月最多调整 3 次；达到上限时阻断保存，Demo 不提供豁免入口。
- 每次调整原因必填，销售可查看调整历史但不可修改；保存动作才写入调整版本和审计。
- 历史已结算不可编辑。
- 销售和月份不可更换。
- 如创建归属错误，不支持删除或改归属，需由管理员按未来校正流程处理。
- 编辑不改变目标状态。

---

## 9. 月内调整确认

### 9.1 进入编辑确认

| 项目 | 规格 |
|------|------|
| 标题 | `调整目标` |
| 内容 | `当前月份已开始，调整目标会立即改变达成率并记录审计。` |
| 取消 | `取消` |
| 确认 | `继续编辑` |

### 9.2 保存前差异确认

| 项目 | 规格 |
|------|------|
| 标题 | `确认调整` |
| 内容 | 展示三项目标修改前后对比 |
| 取消 | `返回修改` |
| 确认 | `确认保存` |
| 调整原因 | Textarea，引用字段清单，必填 |
| 调整次数 | 显示 `本月已调整 {n}/3 次`；达到 3 次时阻断保存 |
| 调整可见性 | 销售本人可查看只读历史；主管/管理员按权限查看 |

规则：

- 仅当前月且目标发生变化时弹出。
- 未来月不弹保存差异确认。
- 变化对比只显示字段名和前后值。
- 上调、下调、零值和超 100% 结果均按字段清单/主 PRD R07-R12 校验。
- 确认后提交当前版本。
- 取消保留表单。

---

## 10. 底部操作栏

### 10.1 布局

- 固定在视口底部。
- 高度 64px。
- 左侧返回。
- 右侧保存。
- 背景白色并有顶部边框。
- 内容底部预留 80px。

### 10.2 按钮

| 按钮 | 样式 | 图标 | 新增文案 | 编辑文案 |
|------|------|------|----------|----------|
| 返回 | 次按钮 | ArrowLeft | 返回 | 返回 |
| 保存 | 主按钮 | Save | 创建目标 | 保存修改 |

按钮规则：

- 保存不修改状态。
- 保存中显示 `保存中...`。
- 保存中禁止重复点击。
- 无权限不展示保存。
- 返回前检查未保存修改。
- 页面无删除按钮。

---

## 11. 保存交互规范

### 11.1 校验顺序

1. 当前用户权限。
2. 编辑模式目标主状态、结算处理状态和锁状态。
3. 销售ID映射。
4. 字段清单校验。
5. 销售+月份唯一性。
6. 目标调整次数与调整原因。
7. 当前月调整确认。
8. 版本并发校验。
9. 保存目标与审计日志。

### 11.2 校验失败

- 所有错误字段同时标红。
- 页面滚动并聚焦第一个错误。
- Toast `请检查并完善目标设置`。
- 销售ID未映射时给出管理员处理提示。
- 重复目标提供现有目标链接。
- 数值错误定位到具体目标控件。

### 11.3 保存成功

新增：

- 后台生成正式目标编号。
- 目标进入进行中。
- 当前进度按当月事件聚合。
- Toast `业绩目标创建成功`。
- 返回目标列表并定位当前月份。

编辑：

- 保存新目标值。
- 记录修改前后审计。
- 状态仍为进行中。
- 结算处理状态仍为 `NOT_STARTED`，不改变锁状态。
- 列表按新目标计算显示比例。
- Toast `业绩目标已更新`。

### 11.4 保存失败

- 保留表单输入。
- 后台目标不变化。
- 当前进度不变化。
- 并发结算时提示 `目标已进入结算，无法继续修改`。
- 结算处理状态为 `FAILED` 时提示 `请返回列表重试结算，不可通过编辑绕过异常`。
- 并发编辑时提示 `目标已被他人更新`。
- 网络结果不确定时按请求键查询最终结果。

---

## 12. 未保存离开

| 项目 | 规格 |
|------|------|
| 触发 | 返回、后退、关闭、切换路由 |
| 标题 | `放弃修改` |
| 内容 | `当前目标设置尚未保存，离开后修改将丢失。` |
| 取消 | `继续编辑` |
| 确认 | `放弃并离开` |

规则：

- 无变化时直接离开。
- 保存成功后不再拦截。
- 放弃离开不创建或修改目标。
- 弹窗提交中不允许重复导航。

---

## 13. 加载、异常与空态

| 场景 | 页面表现 | 动作 |
|------|----------|------|
| 首次加载 | 固定骨架屏 | 等待 |
| 销售查询失败 | 下拉错误态 | 重试 |
| 无可选销售 | 下拉空态 | 联系管理员 |
| 销售ID未映射 | 控件错误提示 | 联系管理员完成映射 |
| 目标不存在 | 404 | 返回列表 |
| 无权限 | 403 | 返回列表 |
| 已结算 | 跳转列表 + Toast | 查看结算快照 |
| 结算锁生效 | 保存阻断 | 返回列表 |
| 结算处理中 | 显示“结算中”只读态 | 返回列表查看任务 |
| 结算异常 | 不进入编辑 | 返回列表点击“重试结算” |
| 保存超时 | 保留表单 | 重试 |

---

## 14. Mock 页面状态

### 14.1 新增当前月目标

- 月份预填当前月。
- 销售为空。
- 选择张三后使用 `S001` 预览编号。
- 唯一性预检通过。
- 三项目标为空。
- 保存后进入进行中。

### 14.2 新增重复目标

- 选择李四与已有目标月份。
- 唯一性预检返回已存在。
- 展示现有目标链接。
- 保存阻断。

### 14.3 未映射销售

- 搜索结果不展示未映射用户。
- 通过旧参数进入时后台阻断。
- 提示联系管理员完成销售ID映射。

### 14.4 当前月编辑

- 销售和月份只读。
- 展示 CRM WON、ERP 确认订单金额、金额差异和两项金额来源。
- 调整任一目标后进度预览变化。
- 保存前展示修改前后对比。
- 保存后主状态仍 `ACTIVE`，结算处理状态仍 `NOT_STARTED`。

### 14.5 未来月编辑

- 销售与月份只读。
- 三项目标可编辑。
- 不显示实时进度或显示初始零值。
- 不弹月内调整确认。

### 14.6 结算并发

- 编辑页打开后模拟月末结算锁。
- 保存返回结算锁冲突。
- 表单不覆盖结算快照。
- 跳转列表显示 `ACTIVE + PROCESSING` 或结算完成后的终态。

---

## 15. 键盘与可访问性

- Tab 顺序与视觉顺序一致。
- 所有控件有可读 Label。
- 错误文案关联控件。
- Select 支持键盘搜索。
- MonthPicker 支持键盘输入。
- NumberInput 支持步进器但可直接输入。
- 进度条同时显示数值。
- 保存结果通过 aria-live 播报。
- 固定底栏不遮挡表单末尾。

---

## 16. 验收清单

- [ ] 每个字段均有控件或明确只读/不展示规则。
- [ ] 销售选择使用销售ID并展示映射。
- [ ] 销售和月份共同生成编号预览。
- [ ] 销售+月份重复目标在预检与保存均阻断。
- [ ] 三项目标可编辑，三项当前值不可编辑。
- [ ] 目标主状态、结算处理状态和锁状态不在表单中出现为可编辑控件。
- [ ] 当前月调整有二次确认和前后值审计。
- [ ] 结算锁生效后保存阻断。
- [ ] 已结算目标不可进入编辑页。
- [ ] 页面无删除目标入口。
- [ ] 目标值范围、精度、零值和科学计数法校验均引用字段清单。
- [ ] 支持目标上调/下调、当前值超 100%、月内最多 3 次调整及调整原因审计。
- [ ] CRM WON 金额与 ERP 确认订单金额并行展示，ERP金额用于结算预览。

---

## 17. 修订记录

| 日期 | 版本 | 变更摘要 |
|------|------|----------|
| 2026-07-18 | V2.0 | 新建新增编辑 Demo，补齐逐字段控件、Placeholder、销售ID与编号联动、唯一性、目标/进度只读边界、月内调整和结算锁 |
