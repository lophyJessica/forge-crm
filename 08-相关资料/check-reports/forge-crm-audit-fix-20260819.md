# AI 自检报告

## 项目/任务

- 项目：Forge CRM 前端原型
- 任务：按 `audit-reviews/CRM对齐审计报告.md` 修复前端代码中的 12 项 P0，并尽量落实 26 项 P1。
- 权威口径：业绩金额只统计 CRM 自己的 WON 金额；公海保护规则服从 `context/`。
- 范围：本轮只修改 `front-prototype/src/`；未修改 PRD、context、标注源文件；未 commit、未 push。

## 修复项清单

### P0（12/12 已清零）

| ID | 修复结果 | 主要改动 |
|---|---|---|
| G-01 | 已修 | 首页 88 分高分线索改为自动分配示例，不再写入培育池。 |
| L-01 | 已修 | 线索草稿和已作废记录不再物理删除，统一保留并写入作废状态。 |
| L-02 | 已修 | 批量作废增加状态、权限、`voidType` 和原因校验，区分经理作废与销售主动放弃。 |
| L-03 | 已修 | 批量导入复用确定性评分和分配规则，80 分及以上自动分配。 |
| P-01 | 已修 | 公海保护期改为原负责人放弃后 7×24 小时内不可再次认领，其他销售不受该保护。 |
| O-01 | 已修 | 商机列表/详情移除手工赢单入口，WON 只由合同签署事件推进。 |
| O-02 | 已修 | 合同签订阶段通过领域动作创建真实合同实体并绑定，不再接受任意合同号形成幽灵合同。 |
| C-02 | 已修 | 已停用/不可用客户详情阻断新建商机、拜访和跟进入口。 |
| K-01 | 已修 | 签署按钮改为明确的外部签署事件 Mock；不创建本地伪 ERP 订单，展示未接入状态。 |
| K-02 | 已修 | 合同归档增加已签署、商机联动、ERP 同步结果、权限、原因和二次确认门槛。 |
| V-01 | 已修 | 已签到拜访仅主管/管理员可取消，取消必须填写至少 10 个字原因并保留签到事实。 |
| T-01 | 已修 | 移除浏览器即时结算和手工月终结算按钮，页面仅展示服务端结算状态。 |

### P1（已落实或明确收敛）

- 线索 L-04/L-05/L-07：评分改为来源、行业、响应速度、历史转化、活跃度五维确定性模型；手机号/邮箱标准化和格式校验覆盖草稿、提交；补齐放弃/作废类型、原因、时间和负责人元数据。
- 线索 L-06：转客户改为 ERP Mock 建档请求后的 CRM 版本化客户快照，保存 `crmCustomerId`、`erpCustomerId`、来源事件和版本。
- 公海 P-02/P-03：收敛合法入池类型和可认领类型；认领增加二次确认、版本、请求键、资格复核和原子更新。
- 商机 O-03/O-04/O-05/O-06：商品改由 ERP Mock 目录提供稳定商品 ID/价格版本；客户使用 CRM 快照双 ID；阶段变更重算确定性评分；商品数量/价格/金额/日期校验补齐；ERP 未接入时明确 `NOT_TRIGGERED`，不制造订单事实。
- 客户 C-01/C-03/C-04/C-05：列表和详情改读 CRM 客户快照；关联优先使用 CRM/ERP 双 ID；AI 风险不再等同 ERP 信用冻结；移除 CRM 本地 ERP 订单伪数据和示例站点跳转。
- 合同 K-03/K-04/K-05：补齐币种、含税、税率、签署事件、请求键、版本、同步状态、作废/归档审计；提交签署统一走校验后的领域动作。
- 拜访 V-02/V-03/V-04/V-05：上门签到明确 Mock 来源，电话/视频不请求定位；补齐结束时间、负责人、执行结果、完成时间、反馈和稳定关联；完成跟进使用固定来源拜访 ID。
- 业绩 T-02：按已定案产品口径只统计 CRM WON；T-03 按销售 ID/事件时间归集并保留旧数据回退；T-04 补齐业务状态与服务端结算状态；T-05 允许 0、限制上限、两位小数并禁止科学计数法；T-06 增加 ACTIVE + NOT_STARTED 编辑路由、调整原因和版本。

仍受前端原型边界限制的 P1 风险：真实 ERP 签署/订单服务、服务端并发锁与服务端时钟、真正的月末结算任务、失败重试队列尚未存在；代码已不再伪造这些外部结果，并在页面显示 Mock/未触发/服务端状态。

### P2（按本轮优先级留待）

G-02、L-08、P-04、O-07、C-06、K-06、V-06、T-07 仍有查询组合、分页、完整 360° 信息架构、动作矩阵或静态示例细节待补，不阻断本轮 P0 主链路。

## 改动文件与改动点

本轮前端代码改动集中在：

- 公共层：`front-prototype/src/db.ts`、`front-prototype/src/App.tsx`。
- 新领域/API 层：`front-prototype/src/domain/businessRules.ts`、`contractActions.ts`、`opportunityActions.ts`、`visitActions.ts`、`front-prototype/src/api/erpCatalog.ts`。
- 页面层：Dashboard、线索 4 页、公海、商机 3 页、客户 2 页、合同 3 页、拜访 3 页、业绩目标 2 页。

## 自检结果

1. `git pull --ff-only`：Already up to date。
2. `git diff --check -- src`：通过。
3. `cd front-prototype && npm run build`：通过；仅有 Vite 大 chunk 提示。
4. `npm run lint`：退出码 0；仅有既有 UI Fast Refresh 和 annotation runtime warning。
5. 打包 `/tmp/forge-crm-fix-20260819182037.zip`：通过；解压第一层直接包含 `index.html`。
6. 部署：rsync 上传后 cron 未消费，按指定备用通道 `bash /root/deploy-all.sh --deploy forge-crm` 发布成功。
7. 线上 HTTP：`https://pmlophy.com/project/forge-crm/` 返回 HTTP 200。
8. 浏览器回归（缓存刷新后）：
   - 业绩目标页不再显示“月终结算”，显示“服务端结算中/待服务端结算”，金额说明为 CRM WON。
   - 商机列表行操作只出现查看/编辑/推进/输单/查看合同，没有手工赢单按钮；ERP 订单说明改为由合同签署事件推进。
   - 已停用客户详情的“新建商机”按钮为 disabled。
   - 已签到拜访行没有销售取消入口；计划态取消打开二次确认，原因少于 10 个字时确认按钮 disabled。
   - “标注清单”打开面板时 class 为 `vpa-panel-toggle is-visible is-active`，关闭后选中态移除，面板正常开合。

## 遗留风险

- 真实后端未接入，ERP 签署、订单草稿、服务端时钟、并发事务和月末结算任务只能以 Mock/状态展示验证。
- 本地 IndexedDB 中已有旧数据可能缺少新字段；代码保留兼容回退，正式联调前仍需数据迁移或清库策略。
- P1 中的外部服务闭环和 P2 列表体验项仍需后续规格/后端协同。

