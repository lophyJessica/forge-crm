# Forge CRM 标注表格渲染修复自检报告

- 项目：Forge CRM
- 分支：`0913test`
- 日期：2026-09-13
- 修复范围：仅修改两份 `runtime.js` 的 `countMarkdownItems` 函数。

## 同步代码

命令：

```text
git pull origin 0913test
```

真实输出：

```text
From https://github.com/lophyJessica/forge-crm
 * branch            0913test   -> FETCH_HEAD
Already up to date.
```

## 代码检查

保留原列表项统计，并新增以 `|` 开头且不是 Markdown 分隔行的表格行统计。

```text
git diff --check
```

真实输出：无输出，命令成功。

Node 计数验证真实输出：

```text
table_rows_including_header=10
list_rows=4
mixed_rows=11
```

其中 `table_rows_including_header=10` 对应 1 个表头和 9 个字段行；分隔行未计入。

## 构建与页面实测

启动命令：

```text
cd /Users/liulongfei/个人文件/forge-crm/99-产品原型prototype
npm run dev
```

5173 已被占用，Vite 使用：`http://localhost:5174/project/forge-crm/`。

构建命令：

```text
npm run build
```

真实输出摘要：

```text
> front-prototype@0.0.0 build
> tsc -b && vite build

vite v8.1.5 building client environment for production...
transforming...✓ 1883 modules transformed.
dist/index.html                   1.49 kB │ gzip:   0.60 kB
dist/assets/index-B5HUUpe2.css   46.85 kB │ gzip:   9.11 kB
dist/assets/index-DnfMPMKu.js   684.09 kB │ gzip: 193.58 kB
✓ built in 229ms
```

浏览器打开线索列表页并展开块 6「线索表格字段与分页」后：

```text
字段说明 10
```

页面 DOM 实测：

```text
activeTab: 全部 19
fieldTab: 字段说明 10
panelHasFieldSection: true
renderedMarkdownTables: 1
```

结论：字段说明 tab 计数非 0，表格内容已渲染；“全部”视图也合并显示了该表格字段说明。

## 提交与推送

提交命令：

```text
git commit -m "fix(标注): countMarkdownItems 统计markdown表格行，修复字段说明转表格后显示为空（0913test）"
```

真实输出：

```text
[0913test d75086a] fix(标注): countMarkdownItems 统计markdown表格行，修复字段说明转表格后显示为空（0913test）
 2 files changed, 8 insertions(+), 2 deletions(-)
```

推送命令：

```text
git push origin 0913test
```

真实输出：

```text
To https://github.com/lophyJessica/forge-crm.git
   c633203..d75086a  0913test -> 0913test
```

## 变更文件

- `99-产品原型prototype/public/annotation-kit/runtime.js`
- `99-产品原型prototype/prototype-annotation/assets/annotation-kit/runtime.js`
