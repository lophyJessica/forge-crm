# Forge CRM 线索 TSV 修复自检报告

## 项目/任务

- 项目：Forge CRM
- 工作区：`/Users/liulongfei/个人文件/forge-crm/`
- 分支：`0913test`
- 任务：走查并修正线索字段详细稿 TSV，对齐《线索主PRD》
- 日期：2026-09-13（Asia/Shanghai）

## 改动文件清单

- 业务文件（唯一修改）：`03-产品设计/第一版-现有基线/字段清单/线索模块/线索字段清单（详细稿）.tsv`
- 报告文件：本报告（commit/push 后生成，未纳入该修复 commit）
- 未修改：线索主 PRD、其他 PRD、annotations、原型代码及其他 TSV。

## 三处必改 diff

```diff
-分配与跟进	归属人	...	分配/认领时写入；一期自动回收时清空；主动放弃时保留，供原负责人立即撤销放弃
+分配与跟进	归属人	...	分配/认领或管理作废时写入/清空；一期自动回收时清空；主动放弃时保留，供原负责人立即撤销放弃

-分配与跟进	入池时间	...	PENDING_ASSIGN 待分配池取进入池时间；主动放弃取 abandonAt
+分配与跟进	入池时间	...	PENDING_ASSIGN 待分配池取进入池时间；主动放弃取放弃动作时间

-系统字段	poolType	...	AI评分≥50分默认 ASSIGN_POOL；AI评分<50分默认 NURTURE_POOL；评分失败按50分进入 ASSIGN_POOL；不构成独立状态
+系统字段	poolType	...	AI评分≥80分触发自动分配→ASSIGNED；50-79分默认 ASSIGN_POOL；<50分默认 NURTURE_POOL；评分失败按50分降级进 ASSIGN_POOL；不构成独立状态
```

## 四项查证结论

1. **联系人必填性**：保持 `选填`。PRD S01（第 4 节，约第 113 行）仅描述手动录入 `联系人`，未明确“必填”；R02（第 6.1 节）将必填性归于《线索字段清单》，未提供反向口径。结论：**待确认（PRD未明示）**。
2. **手机号/邮箱至少填一个**：保持现有“与邮箱至少填一个；”“与手机号至少填一个；”备注。PRD 第 2 节、第 9.2 节及 S07/V01 仅定义手机号和邮箱的去重、清洗与格式校验，未定义至少填写一个的约束。结论：**待确认（PRD未定义该约束）**。
3. **线索来源/所属行业枚举**：保持现有 `ONLINE/ACTIVITY/EXHIBITION/REFERRAL/IMPORT/OTHER` 与 `MANUFACTURING/RETAIL/HEALTHCARE/FINANCE/IT/OTHER`。PRD 仅在 V02 以 `ONLINE`、`MANUFACTURING` 作为测试输入示例，并未定义完整枚举清单。结论：**待确认（枚举无PRD依据）**。
4. **线索编号格式**：保持现有 `LEAD{YYYYMMDD}-{4位序号}`。PRD 第 5.3 节只要求生成唯一 `线索编号`，V01 使用 `LEAD20260717-0001` 作为示例，未定义规范格式。结论：**待确认（PRD未定义编号规范）**。

## 结构与 Git 自检

- TSV 表头 13 列；总行数 25（表头 + 24 条字段行），修复前后均未变化。
- `git diff --cached --check`：通过。
- commit：`cd8641c`，仅 1 个文件、3 行新增/3 行删除。
- commit 信息：`fix(字段清单): 修正线索详细稿 poolType分池边界与放弃/归属口径，对齐主PRD`。
- `git push origin 0913test`：成功（`f74e271..cd8641c`）。
- 推送后 `HEAD` 与 `origin/0913test` 均为 `cd8641c`。
- 推送后工作树无已跟踪修改；本报告为唯一新增未跟踪文件。

## 遗留风险

- 四项查证均未擅自新增 PRD 未定义口径，需产品确认后再同步字段清单或 PRD。
- 本次未修改联系人必填性、联系方式条件必填、枚举清单和编号格式，避免臆造业务规则。
