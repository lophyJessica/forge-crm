# Forge CRM 框架合并自检报告

## 项目/任务

- 项目：Forge CRM
- 分支：`0913test`
- 任务：消除旧目录与 QS-JXC 分层并存，收敛为唯一 00~08、99 顶层结构
- 审计时间：2026-09-13（Asia/Shanghai）

## 改动文件清单

- 已跟踪文件：无改动。
- 新增未跟踪文件：本报告。
- 忽略文件清理：根目录 `.DS_Store`、`forge-crm.zip`；`context/.DS_Store`、`docs/.DS_Store`、`prd-docs/.DS_Store`、`prompt/.DS_Store`；旧 `front-prototype/dist/`（11 个文件）与 `front-prototype/node_modules/`（6130 个文件）。
- 空目录清理：`context/`、`docs/`、`prd-docs/`、`front-prototype/`、`prompt/`、`prompt/第二阶段/`、`drafts/`、`reference/`、`prototypes/`。`template/` 原本不存在。

## 改动点说明

### 映射结果

本次审计显示旧目录均无 Git 已跟踪文件，也无符合 Git 的非忽略 untracked 文件，因此没有可执行的 `git mv`；现有 QS-JXC 目标目录内容保持不变。

| 旧目录 | 目标目录 | 移动文件数 | 结果 |
|---|---|---:|---|
| `context/` | `01-全局背景信息/` | 0 | 源目录仅有 `.DS_Store`，已按确认清理 |
| `docs/` | `02-需求调研/项目资料与文案/` | 0 | 源目录仅有 `.DS_Store`，已按确认清理；目标路径未新增内容 |
| `prd-docs/` | `03-产品设计/第一版-现有基线/需求文档与规则/` | 0 | 源目录仅有 `.DS_Store`，已按确认清理；未发生同名覆盖 |
| `template/` | `04-常用模版/` | 0 | 源目录不存在；目标目录保持现状 |
| `prompt/` | `05-提示词合集/` | 0 | 源目录仅有 `.DS_Store` 与空目录，已按确认清理 |
| `drafts/` | `00-草稿对话/` | 0 | 空目录，已按确认清理 |
| `reference/` | `08-相关资料/` | 0 | 空目录，已按确认清理 |
| `front-prototype/` 内容 | `99-产品原型prototype/` 根 | 0 | 源目录仅含构建产物与依赖；未迁移，避免污染 buildable 根 |
| `prototypes/` | 暂不并入 | 0 | 空目录，清单为空，已按确认清理 |

### 同名冲突处置

- 旧目录没有业务文件，未发现同名冲突。
- 未覆盖或丢弃目标目录中的任何已跟踪内容。

### untracked 清单与处置

- 初始 `git status --short`：为空。
- 初始 `git ls-files --others --exclude-standard`：为空。
- 初始残留均为 ignored 项，已在用户确认后清理；没有静默丢弃非忽略文件。
- 报告生成后新增的非忽略 untracked：本报告文件（未暂存）。
- 构建后新增 ignored：`99-产品原型prototype/node_modules/` 与 `99-产品原型prototype/dist/`，用于本地构建验证，不纳入提交。

## 自检结果

- `git checkout 0913test`：成功，已在目标分支。
- `git pull --ff-only`：成功，`Already up to date`。
- `npm install`（`99-产品原型prototype/`）：成功；npm 报告 4 个依赖漏洞（1 moderate、3 high），未执行 `npm audit fix`。
- `npm run build`（`99-产品原型prototype/`）：通过；生成 `dist/index.html`、JS/CSS bundle 与 annotation-kit 资产。
- 顶层旧目录检查：`context/`、`prompt/`、`prd-docs/`、`front-prototype/`、`template/`、`drafts/`、`reference/`、`prototypes/`、`docs/` 均不存在。
- `git status --short`：仅本报告为 `??`，无已跟踪修改。

## 遗留风险

- npm 依赖审计存在 4 个漏洞（1 moderate、3 high），本任务未升级依赖。
- Vite 提示主 bundle 压缩后约 684 kB，超过 500 kB 建议阈值；不影响本次构建通过。
- 旧 `front-prototype/` 没有可迁移源码；如未来发现外部未纳入工作区的源码，需要另行评估来源与合并策略。
- 本次仅完成工作区清理与构建验证，未 commit、未 push、未部署。
