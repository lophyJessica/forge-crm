# Forge CRM 表单标注区域级修复自检报告

- 项目：Forge CRM
- 分支：`0913test`
- 日期：2026-09-13
- 任务：将商机、合同、拜访、业绩目标四个表单页的基本信息标注从字段级收敛为区域级。

## 1. 拉取基线

执行命令：

```text
git pull origin 0913test
```

真实输出：

```text
From https://github.com/lophyJessica/forge-crm
 * branch            0913test   -> FETCH_HEAD
Already up to date.
```

## 2. 改动范围与结果

仅涉及四个表单页的 annotations Markdown、模块 `annotation.config.json`、对应 React 页面锚点，以及编译生成的 bundle/coverage 文件；未修改业务逻辑。

| 表单页 | 原块数 | 合并后块数 | 新增区域锚点 | 移除字段级锚点 | 保留锚点 |
|---|---:|---:|---|---|---|
| 商机新增/编辑 | 9 | 5 | `opportunity-form-basic` | `opportunity-form-title-field`、`opportunity-form-customer-field`、`opportunity-form-forecast-fields`、`opportunity-form-product-fields`、`opportunity-form-requirement-field` | `opportunity-form-page-header`、`opportunity-form-validation`、`opportunity-form-submit-bar`、`opportunity-form-save-action` |
| 合同新增/编辑 | 9 | 5 | `contract-form-basic` | `contract-form-opportunity-field`、`contract-form-customer-inheritance`、`contract-form-title-field`、`contract-form-amount-field` | `contract-form-page-header`、`contract-form-footer`、`contract-form-save-draft`、`contract-form-submit-sign` |
| 拜访计划新增/编辑 | 8 | 4 | `visit-form-basic` | `visit-form-association-fields`、`visit-form-title-field`、`visit-form-method-time-fields`、`visit-form-address-field` | `visit-form-page-header`、`visit-form-footer`、`visit-form-save-action` |
| 业绩目标新增/编辑 | 8 | 3 | `target-form-basic` | `target-form-sales-field`、`target-form-month-field`、`target-form-lead-target-field`、`target-form-opp-target-field`、`target-form-amount-target-field` | `target-form-page-header`、`target-form-footer` |

基本信息区域的字段说明已合并为表格并去重重复条件规则；字段内容沿用对应字段清单与原有需求来源。各区域级 config 条目聚合了原字段条目的 `sourceRefs`：商机 5 个、合同 5 个、拜访 5 个、业绩目标 6 个；页面头、校验、底部操作和保存/提交动作的 sourceRefs 保持覆盖。

## 3. Bundle 块数核对

对 `public/annotation-kit/annotation.bundle.json` 执行实际核对：

```text
opportunities: bundleFormBlocks=5, expectedAreaLevelBlocks=5, result=PASS
contracts: bundleFormBlocks=5, expectedAreaLevelBlocks=5, result=PASS
visits: bundleFormBlocks=4, expectedAreaLevelBlocks=4, result=PASS
targets: bundleFormBlocks=3, expectedAreaLevelBlocks=3, result=PASS
```

## 4. 编译与资源校验

执行命令：

```text
cd /Users/liulongfei/个人文件/forge-crm/99-产品原型prototype
python3 prototype-annotation/scripts/compile_annotations.py "../03-产品设计/20260913-第一版-现有基线/需求文档与规则/annotation.workspace.json" --output "public/annotation-kit/annotation.bundle.json"
```

真实输出：

```text
Compiled annotation bundle: /Users/liulongfei/个人文件/forge-crm/99-产品原型prototype/public/annotation-kit/annotation.bundle.json
Wrote coverage matrix: /Users/liulongfei/个人文件/forge-crm/03-产品设计/20260913-第一版-现有基线/需求文档与规则/annotation-coverage.md
```

执行命令：

```text
python3 prototype-annotation/scripts/check_annotation_assets.py ./public/annotation-kit
```

真实输出：

```text
Deployment assets valid: {"annotations": 123, "pages": 17, "version": 1, "coverage": {"total": 149, "mapped": 149, "unmapped": 0}}
```

## 5. 生产构建

执行命令：

```text
npm run build
```

真实输出：

```text
> front-prototype@0.0.0 build
> tsc -b && vite build

vite v8.1.5 building client environment for production...
transforming...✓ 1883 modules transformed.
rendering chunks...
computing gzip size...
dist/index.html                   1.49 kB │ gzip:   0.60 kB
dist/assets/index-B5HUUpe2.css   46.85 kB │ gzip:   9.11 kB
dist/assets/index-BY-fGaai.js   683.19 kB │ gzip: 193.23 kB

✓ built in 210ms
[plugin builtin:vite-reporter]
(!) Some chunks are larger than 500 kB after minification. Consider:
- Using dynamic import() to code-split the application
- Using code.rolldownOptions.output.codeSplitting to improve chunking: https://rolldown.rs/reference/OutputOptions.codeSplitting
- Adjusting build.chunkSizeWarningLimit to change this limit.
```

构建成功；大 chunk 为既有构建警告，不影响本次校验结果。

## 6. 提交与推送

提交命令：

```text
git commit -m "fix(标注): 四个表单基本信息改为区域级标注（0913test）"
```

真实输出：

```text
[0913test 5e8199c] fix(标注): 四个表单基本信息改为区域级标注（0913test）
 14 files changed, 252 insertions(+), 1005 deletions(-)
```

推送命令：

```text
git push origin 0913test
```

真实输出：

```text
To https://github.com/lophyJessica/forge-crm.git
   47ef707..5e8199c  0913test -> 0913test
```

结论：四个表单页均已完成区域级标注改造，bundle 块数与目标一致，sourceRefs 全量映射，编译、资源校验和生产构建通过。
